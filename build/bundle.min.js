function Y(Y) {
    QF = Y >>> 0;
}

function B() {
    QF = QF + 1831565813 | 0;
    let Y = JE.imul(QF ^ QF >>> 15, 1 | QF);
    Y = Y + JE.imul(Y ^ Y >>> 7, 61 | Y) ^ Y;
    return ((Y ^ Y >>> 14) >>> 0) / 4294967296;
}

function C(Y, B) {
    let C = JE.imul(Y | 0, 374761393) + JE.imul(B | 0, 668265263);
    C = JE.imul(C ^ C >>> 13, 1274126177);
    return ((C ^ C >>> 16) >>> 0) / 4294967296;
}

function D(Y, B, C, D, E, F) {
    const H = C - Y, I = D - B, J = H * H + I * I;
    return J ? NF(((E - Y) * H + (F - B) * I) / J, 0, 1) : 0;
}

function E(Y, B, C, D, E, F, H, I) {
    const J = C - Y, K = D - B, L = H - E, M = I - F, N = J * M - K * L;
    if (!N) {
        return null;
    }
    const O = ((E - Y) * M - (F - B) * L) / N, P = ((E - Y) * K - (F - B) * J) / N;
    return O >= 0 && O <= 1 && P >= 0 && P <= 1 ? [ Y + J * O, B + K * O ] : null;
}

function F(Y, B, C, D, E, F, H) {
    const I = ME(3, (F - E) * D / 80 | 0);
    for (let J = 0; J < I; J++) {
        const K = OF(E, F, J / I), L = OF(E, F, (J + 1) / I);
        Y.o.push(QJ(B + OE(K) * D, C + NE(K) * D, B + OE(L) * D, C + NE(L) * D, H));
    }
}

function H(Y) {
    VJ = Y.x;
    TJ = Y.y;
    UJ = 0;
    RJ = 0;
    WJ = Y.g;
    if (Y.os) {
        const B = Y.os * EH + Y.op, C = NE(B), D = OE(B) * Y.os;
        VJ += Y.ox * C;
        TJ += Y.oy * C;
        UJ = Y.ox * D;
        RJ = Y.oy * D;
    }
    Y.w && (WJ += Y.w * EH);
}

function I(Y, B, C) {
    if (Y.w) {
        UJ += -Y.w * (C - TJ);
        RJ += Y.w * (B - VJ);
    }
}

function J(Y, B) {
    return TF(AI(Y, 2)) ? 2 : B && TF(AI(Y, 0)) ? 4 : TF(AI(Y, 1)) ? 8 : TF(AI(Y, 3)) ? 1 : 0;
}

function K(Y, B) {
    if (!TF(AI(Y, 4))) {
        return null;
    }
    const C = SF(0, IE);
    return {
        ox: OE(C) * B,
        oy: NE(C) * B * .6,
        os: SF(.7, 2.1),
        op: SF(0, IE)
    };
}

function L(Y, B, C) {
    if (!Y.t) {
        ZJ = B - VJ;
        YK = C - TJ;
        BK = QE(ZJ, YK);
        if (BK < 1e-4) {
            ZJ = 0;
            YK = -1;
            BK = 1e-4;
        } else {
            ZJ /= BK;
            YK /= BK;
        }
        XJ = VJ + ZJ * Y.r;
        AJ = TJ + YK * Y.r;
        return Y.r;
    }
    const E = OE(WJ) * Y.r, F = NE(WJ) * Y.r, H = VJ - E, I = TJ - F, J = VJ + E, K = TJ + F, L = D(H, I, J, K, B, C);
    XJ = H + (J - H) * L;
    AJ = I + (K - I) * L;
    ZJ = B - XJ;
    YK = C - AJ;
    BK = QE(ZJ, YK);
    if (BK < 1e-4) {
        ZJ = -NE(WJ);
        YK = OE(WJ);
        BK = 1e-4;
    } else {
        ZJ /= BK;
        YK /= BK;
    }
    return 5;
}

function M(Y, B, C, D) {
    for (const E of Y.o) {
        VJ = E.x;
        TJ = E.y;
        WJ = E.g;
        if (L(E, B, C) + D > BK) {
            return 1;
        }
    }
    return 0;
}

function N() {
    const Y = BJ, B = CK(Y), C = EK(Y), D = XI(B), E = SE((Y + 1400) / FF) > SE(Y / FF) && Y > 200, F = TF(.26) ? 120 : 0, H = NF(-400 + SF(-90, 70) - F, -545, -300), I = NF(WE + SF(-70, 90) + F, 300, XE), J = E ? 1240 : VF(760, 1080), K = {
        y: Y,
        h: J,
        l: H,
        r: I,
        pl: CJ,
        pr: DJ,
        o: [],
        i: [],
        rg: B,
        k: 0,
        v: 0,
        z: 0,
        bk: []
    };
    K.o.push(QJ(CJ, Y, H, Y + J, 0), QJ(DJ, Y, I, Y + J, 0));
    IJ = ME(CJ, H) + 30;
    JJ = LE(DJ, I) - 30;
    KJ = JJ - IJ;
    LJ = (IJ + JJ) / 2;
    MJ = D;
    NJ = C;
    if (E) {
        Z(K);
    } else if (EJ > 2 && TF(.1)) {
        K.v = 1;
        A(K, B);
    } else {
        const Y = +TI[B][5][VF(0, 9)];
        K.k = Y;
        EJ > 1 && VF(0, 9) < TI[B][8] && (K.z = YJ(B) || VF(1, 5));
        Y === 1 || Y === 5 ? S(K, Y > 1 ? VF(2, 3) : 1, Y > 1 ? SF(0, 26) : SF(120, 210), K.y + K.h * SF(.34, .6), 0) : [ Q, 0, V, T, U, 0, R, W, X ][Y](K);
        O(K, TI[B][6] + (C * 1.6 | 0));
        YY(K, B);
    }
    K.i = K.i.filter(Y => !M(K, Y.x, Y.y, 10.2));
    CJ = H;
    DJ = I;
    BJ = Y + J;
    EJ++;
    DI.push(K);
    DI.length > 26 && DI.shift();
    return K;
}

function O(Y, B) {
    for (let C = B * 6; B > 0 && C--; ) {
        const C = SF(IJ + 44, JJ - 44), D = Y.y + SF(.07, .93) * Y.h, E = TF(.58), F = SF(13, 25), H = SF(30, 76), I = SF(0, HE), L = 48 + (E ? F : 5);
        if (!M(Y, C, D, L)) {
            if (!E) {
                const B = OE(I) * H, E = NE(I) * H;
                if (M(Y, C + B, D + E, L) || M(Y, C - B, D - E, L)) {
                    continue;
                }
            }
            B--;
            Y.o.push(E ? OJ(C, D, F, J(MJ, 0) | (TF(.5) ? 1 : TF(.3) ? EF : 0), K(MJ, 74)) : PJ(C, D, H, I, J(MJ, 1) | (TF(.22) ? 16 : 0), K(MJ, 62)));
            Y.o[Y.o.length - 1].m & EF && P(Y, Y.o[Y.o.length - 1], 3);
        }
    }
}

function P(Y, B, C) {
    let D = Y.bk[Y.bk.length - 1];
    if (!D || D.n >= D.cap) {
        D = {
            n: 0,
            l: 0,
            cap: C,
            x: B.x,
            y: B.y,
            m: []
        };
        Y.bk.push(D);
    }
    D.n++;
    D.m.push(B);
    B.bk = D;
    B.m |= EF;
}

function Q(Y) {
    const B = VF(4, 6), C = Y.h / (B + 1);
    for (let D = 0; D < B; D++) {
        const B = Y.y + C * (D + 1), E = VF(4, 7);
        for (let C = 0; C < E; C++) {
            Y.o.push(OJ(IJ + 44 + (C + (D & 1 ? .5 : 0)) / (E - .35) * (KJ - 88), B, SF(14, 27), J(MJ, 0) | (TF(.45) ? 1 : TF(.25) ? EF : 0), K(MJ, 60)));
            Y.o[Y.o.length - 1].m & EF && P(Y, Y.o[Y.o.length - 1], 3);
        }
    }
}

function S(Y, B, C, D, E) {
    const F = J(MJ, 1), H = [];
    for (let Y = 0; Y < B; Y++) {
        H.push(IJ + (Y + SF(.25, .75)) * KJ / B);
    }
    let I = IJ - 20, K = D - C;
    for (let E = 0; E <= B; E++) {
        const J = E === B, L = ME(100, SF(125, 195) - NJ * 16), M = J ? JJ + 20 : H[E] - L / 2, N = J ? D - C : D + C;
        M - I > 24 && Y.o.push(QJ(I, K, M, N, F));
        !J && C > 40 && TF(.6) && Y.o.push(OJ(M, N, 15, 1));
        I = J ? M : H[E] + L / 2;
        K = D + C;
    }
    if (!E) {
        for (let B = 0; B < 4; B++) {
            Y.i.push(SJ(0, H[0] + SF(-26, 26), D + C + 70 + B * 46));
        }
        Y.i.push(SJ(TF(.35) ? 2 : 1, H[B - 1], D - C - 70, UF(ZI(Y.rg))));
    }
}

function V(Y) {
    const B = LE(KJ * .42, 280), C = LJ + SF(-.2, .2) * KJ, D = Y.y + Y.h * .58, E = B * OE(.12 * HE);
    F(Y, C, D, B, .12 * HE, .88 * HE, TF(.35) ? 2 : 0);
    Y.o.push(OJ(C - E, D, 15, 1), OJ(C + E, D, 15, 1));
    Y.i.push(SJ(TF(.3) ? 1 : 2, C, D + B * .55, UF(ZI(Y.rg))));
    TF(.6) && Y.o.push(PJ(C, Y.y + Y.h * .18, SF(60, 120), SF(-.4, .4), 1, K(MJ, 90)));
}

function T(Y) {
    const B = ME(135, 250 - NJ * 30), C = LJ + SF(-.22, .22) * KJ, D = SF(-90, 90);
    Y.o.push(QJ(C - B / 2, Y.y, C - B / 2 + D, Y.y + Y.h, 32));
    Y.o.push(QJ(C + B / 2, Y.y, C + B / 2 + D, Y.y + Y.h, 32));
    for (let B = 0; B < 7; B++) {
        Y.i.push(SJ(0, C + D * (B / 7) + SF(-30, 30), Y.y + 80 + B * (Y.h - 160) / 6));
    }
    if (TF(.6)) {
        const B = C < LJ ? JJ - 70 : IJ + 70, D = TF(.45);
        Y.i.push(SJ(D ? 2 : 4, B, Y.y + Y.h * .5, D ? UF(ZI(Y.rg)) : VF(0, 6)));
    }
}

function U(Y) {
    const B = VF(1, 2);
    for (let C = 0; C < B; C++) {
        const D = LJ + SF(-.25, .25) * KJ, E = Y.y + Y.h * (B === 1 ? .5 : .3 + C * .42), F = VF(2, 3), H = LE(KJ * .34, 210), I = RF() * SF(.9, 1.9 + NJ * .3);
        for (let B = 0; B < F; B++) {
            Y.o.push(PJ(D, E, H, B * HE / F, 1, {
                w: I
            }));
        }
        Y.o.push(OJ(D, E, 18, 0));
        Y.i.push(SJ(0, D, E - H - 40), SJ(0, D, E + H + 40));
    }
}

function R(Y) {
    const C = Y.y + Y.h * .16, D = Y.y + Y.h * .86;
    S(Y, 1, 0, C, 1);
    S(Y, 1, 0, D, 1);
    const E = VF(4, 7);
    for (let F = 0; F < E; F++) {
        const E = IJ + SF(.12, .88) * KJ, F = OF(C + 60, D - 60, B());
        M(Y, E, F, 66) || (TF(.45) ? Y.o.push(OJ(E, F, SF(16, 30), TF(.5) ? 1 : 16, K(MJ, 80))) : Y.o.push(PJ(E, F, SF(46, 96), SF(0, HE), J(MJ, 1) | (TF(.3) ? 32 : 0), K(MJ, 70))));
    }
    const F = LJ, H = (C + D) / 2, I = LE(KJ * .3, 190);
    for (let B = 0; B < 4; B++) {
        const C = B / 4 * IE + SF(0, 1), D = F + OE(C) * I, E = H + NE(C) * I * .7;
        M(Y, D, E, 62) || Y.o.push(OJ(D, E, SF(13, 21), 1, K(MJ, 56)));
    }
    Y.i.push(SJ(TF(.25) ? 1 : 2, TF(.5) ? IJ + 55 : JJ - 55, (C + D) / 2, UF(ZI(Y.rg))));
}

function W(Y) {
    const B = VF(2, 3);
    for (let C = 0; C < B; C++) {
        const D = Y.y + Y.h * (.2 + C * .62 / B + SF(-.04, .04)), E = VF(3, 5), F = LE(KJ * .74, 330), H = LJ - F / 2 + SF(-40, 40), I = {
            n: E,
            l: 0,
            cap: E,
            x: LJ,
            y: D,
            m: []
        };
        Y.bk.push(I);
        for (let B = 0; B < E; B++) {
            const C = PJ(H + (B + .5) * F / E, D, 30, HE / 2 + SF(-.2, .2), EF);
            C.bk = I;
            I.m.push(C);
            Y.o.push(C);
        }
        Y.o.push(OJ(H - 42, D, 20, 1), OJ(H + F + 42, D, 20, 1));
        for (let B = 0; B < E; B++) {
            Y.i.push(SJ(0, H + (B + .5) * F / E, D - 54));
        }
    }
    Y.i.push(SJ(TF(.4) ? 4 : 2, LJ + SF(-.3, .3) * KJ, Y.y + Y.h * .92, VF(0, 6)));
}

function X(Y) {
    const B = LE(KJ * .8, 380), C = LJ + SF(-.1, .1) * KJ, D = VF(3, 4);
    for (let E = 0; E < D; E++) {
        const F = Y.y + Y.h * (.14 + E * .74 / D), H = C + SF(-.34, .34) * B, I = ME(86, 150 - NJ * 20);
        for (const D of [ -1, 1 ]) {
            const E = D < 0 ? C - B / 2 : C + B / 2, J = H + D * I / 2;
            KE(E - J) > 30 && Y.o.push(QJ(J, F, E, F + SF(-14, 14), 4));
        }
        Y.o.push(PJ(H, F + Y.h * .37 / D, LE(B * .34, 150), SF(0, HE), 1, {
            w: RF() * SF(.8, 1.7 + NJ * .4)
        }));
        Y.i.push(SJ(0, H, F + 40), SJ(0, H + SF(-30, 30), F + 86));
    }
    Y.i.push(SJ(TF(.5) ? 1 : 4, C, Y.y + Y.h * .96, VF(0, 6)));
}

function A(Y, B) {
    const C = LJ, D = Y.y + Y.h * .5, E = LE(KJ * .38, 250);
    Y.cx = C;
    Y.cy = D;
    F(Y, C, D, E, .78 * HE, 2.16 * HE, 1);
    const H = TF(.4);
    for (let B = 0; B < 3; B++) {
        Y.o.push(PJ(C, D, E * .46, B * HE / 3, 1, {
            w: RF() * .55
        }));
    }
    Y.i.push(SJ(H ? 3 : 1, C, D - E * .72));
    Y.i.push(SJ(2, C - E * .6, D + E * .3, UF(ZI(B))));
    Y.i.push(SJ(2, C + E * .6, D + E * .3, VF(0, 6)));
    for (let B = 0; B < 6; B++) {
        Y.i.push(SJ(0, C + OE(B / 6 * IE) * E * .78, D + NE(B / 6 * IE) * E * .78));
    }
}

function Z(Y) {
    const B = LJ, C = Y.y + Y.h * .4;
    U(Y);
    S(Y, 1, 0, C + 300, 1);
    Y.o.push(OJ(B, C, 26, 1));
    for (let D = 0; D < 7; D++) {
        Y.i.push(SJ(2, B + 74 * (D - 3), C + 420, D));
    }
    Y.i.push(SJ(1, B, Y.y + 150));
}

function YY(Y, B) {
    const C = ZI(B), D = TI[B][6] / 6, E = (B, C, D, E) => {
        for (let F = 0; F < 10; F++) {
            const F = SF(IJ + 45, JJ - 45), H = Y.y + SF(D, E) * Y.h;
            if (!M(Y, F, H, 34)) {
                Y.i.push(SJ(B, F, H, C));
                return;
            }
        }
    };
    if (TF(.75)) {
        const B = VF(5, 9), C = IJ + SF(.1, .5) * KJ, D = Y.y + SF(.15, .5) * Y.h, E = RF(), F = SF(150, 330), H = SF(-190, 240);
        for (let I = 0; I < B; I++) {
            const J = I / (B - 1), K = C + E * F * J, L = D + H * NE(J * HE) + J * 150;
            K > IJ + 25 && K < JJ - 25 && !M(Y, K, L, 26) && Y.i.push(SJ(0, K, L));
        }
    }
    TF(.62 * D) && E(2, TF(.7) ? UF(C) : VF(0, 6), .15, .85);
    TF(.3 * D) && E(TF(.3) ? 4 : 1, VF(0, 6), .02, .16);
    TF(.16 * D) && E(4, VF(0, 6), .3, .8);
    if (TF(.24 * D)) {
        const B = TF(.5) ? IJ + 90 : JJ - 90, C = Y.y + SF(.25, .7) * Y.h;
        for (let D = 0; D < 4; D++) {
            Y.o.push(PJ(B + (D < 2 ? 0 : D > 2 ? 62 : -62), C + (D < 2 ? D ? 62 : -62 : 0), 62, D < 2 ? HE / 2 : 0, 4));
        }
        Y.i.push(SJ(0, B, C), SJ(0, B - 34, C), SJ(0, B + 34, C), SJ(TF(.4) ? 1 : 2, B, C - 34, VF(0, 6)));
    }
}

function BY(B) {
    FJ = B || VE() * 1e9 | 0;
    Y(FJ);
    DI = [];
    BJ = -900;
    CJ = -400;
    DJ = WE;
    EJ = 0;
    HJ = null;
    const C = {
        y: -900,
        h: 1400,
        l: -400,
        r: WE,
        pl: -400,
        pr: WE,
        o: [],
        i: [],
        rg: 0,
        k: 0,
        v: 0,
        z: 0,
        bk: []
    };
    C.o.push(QJ(-400, -900, -400, 500, 0), QJ(WE, -900, WE, 500, 0), QJ(-400, -900, WE, -900, 1), OJ(-140, 60, 26, 1), OJ(150, 250, 26, 1), OJ(-60, 420, 22, 1));
    for (let Y = 0; Y < 7; Y++) {
        C.i.push(SJ(0, Y * 74 - 220, 150 + NE(Y * .9) * 90));
    }
    C.i.push(SJ(2, 250, -60, 1), SJ(2, -260, 380, 2));
    DI.push(C);
    BJ = 500;
    while (BJ < 3e3) {
        N();
    }
}

function CY() {
    while (BJ < LH.y + 3200) {
        N();
    }
    const Y = DI[0];
    if (LH.y < Y.y + 40) {
        LH.y = Y.y + 40;
        LH.vy < 0 && (LH.vy = KE(LH.vy) * .6);
    }
}

function DY(Y) {
    FK = -545;
    HK = XE;
    for (const B of DI) {
        if (Y >= B.y && Y < B.y + B.h) {
            const C = (Y - B.y) / B.h;
            FK = OF(B.pl, B.l, C);
            HK = OF(B.pr, B.r, C);
            return;
        }
    }
}

function EY(Y, B, C) {
    IK = JK = 0;
    const D = Y.z;
    if (!D) {
        return;
    }
    const E = (C - Y.y) / Y.h, F = NE(E * 7 + EH * 1.4);
    if (D === 1) {
        const C = OE((B - Y.l) / (Y.r - Y.l) * IE * 1.5);
        C > .1 && (JK = -1437.5 * C);
    } else if (D === 2) {
        const D = (Y.l + Y.r) / 2 - B, E = Y.y + Y.h * .5 - C, F = QE(D, E) + 60, H = LE(1, 300 / F) * UE * .8;
        IK = D / F * H;
        JK = E / F * H;
    } else if (D === 3) {
        IK = F * UE * .62;
    } else if (D === 4) {
        JK = -2500 * NF(LE(E, 1 - E) * 6, 0, 1);
    } else if (D === 5) {
        const D = B - (Y.l + Y.r) / 2, E = C - (Y.y + Y.h * .5), F = QE(D, E) + 80, H = 875 * LE(1, 340 / F);
        IK = -E / F * H;
        JK = D / F * H;
    } else {
        const D = (Y.l + Y.r) / 2 + NE(C * .0016 + Y.y * .01) * (Y.r - Y.l) * .3;
        IK = NF(5 * (D - B), -1250, UE);
        JK = 687.5;
    }
}

function FY(Y, B) {
    const C = [];
    for (const D of DI) {
        D.y + D.h > Y && D.y < B && C.push(D);
    }
    return C;
}

function HY() {
    const Y = QE(LH.vx, LH.vy);
    if (Y > RE) {
        LH.vx = LH.vx / Y * RE;
        LH.vy = LH.vy / Y * RE;
    }
    if (!isFinite(Y)) {
        LH.vx = 0;
        LH.vy = 200;
    }
    if (!isFinite(LH.x + LH.y)) {
        LH.x = 0;
        LH.y = XH;
    }
}

function IY(Y) {
    const B = LH.ra;
    if (B) {
        B.u = 1;
        B.l = LE(B.l, .12);
    }
    LH.ra = null;
    if (Y) {
        LH.vx *= 1.06;
        LH.vy *= 1.06;
    }
    PB(0);
}

function JY(Y) {
    const B = LH.ra, C = B.x2 - B.x1, D = B.y2 - B.y1, E = QE(C, D);
    if (B.u || E < 8) {
        IY(1);
        return 0;
    }
    const F = C / E, H = D / E;
    let I = LH.vx * F + LH.vy * H;
    I = I * .998 + (MH * F + NH * H) * Y;
    if (KE(I) < 370) {
        const Y = MH * F + NH * H;
        I = 370 * (Y ? Y > 0 ? 1 : -1 : I >= 0 ? 1 : -1);
    }
    LH.rt += I * Y / E;
    LH.vx = F * I;
    LH.vy = H * I;
    if (LH.rt < 0 || LH.rt > 1) {
        LH.rt = NF(LH.rt, 0, 1);
        if (!(LH.rw > 0)) {
            IY(1);
            return 0;
        }
        LH.rw -= Y;
        LH.vx = -LH.vx;
        LH.vy = -LH.vy;
        LH.rt = NF(LH.rt, .02, .98);
        NB(400);
    }
    const J = F * LH.rs;
    LH.x = B.x1 + C * LH.rt + -H * LH.rs * 22;
    LH.y = B.y1 + D * LH.rt + J * 22;
    VE() < .5 && LC(LH.x, LH.y, 3, 0, 60, CF[4], .1 * -LH.vx, .1 * -LH.vy);
    return 1;
}

function KY() {
    if (!LH.te) {
        return;
    }
    LH.te = null;
    const Y = 1.34 + 150 / (QE(LH.vx, LH.vy) || 1);
    LH.vx *= Y;
    LH.vy *= Y;
    HY();
    LC(LH.x, LH.y, 10, 0, 260);
    OB(0);
}

function LY() {
    const Y = LH.te;
    let B = LH.x - Y.x, C = LH.y - Y.y;
    const D = QE(B, C);
    if (D < 1 || D <= Y.l) {
        return;
    }
    B /= D;
    C /= D;
    LH.x = Y.x + B * Y.l;
    LH.y = Y.y + C * Y.l;
    const E = LH.vx * B + LH.vy * C;
    if (E > 0) {
        LH.vx -= E * B;
        LH.vy -= E * C;
    }
    const F = QE(LH.vx, LH.vy);
    if (F > 1 && F < 1500) {
        const Y = ME(1.004, 320 / F);
        LH.vx *= Y;
        LH.vy *= Y;
    }
}

function MY(Y) {
    const B = LF(4) ? 180 + WH * 24 : WH > 4 ? 60 + WH * 24 : 0;
    for (const C of KK) {
        for (const D of C.i) {
            if (D.g) {
                continue;
            }
            const C = LH.x - D.x, E = LH.y - D.y, F = QE(C, E);
            if (F < 43) {
                CB(D);
            } else if (B && !D.t && F < B) {
                const B = LE(1, 700 * Y / F);
                D.x += C * B;
                D.y += E * B;
            }
        }
    }
}

function NY(Y) {
    for (const Y of KK) {
        for (const B of Y.o) {
            B.k || OY(B);
        }
    }
    if (LK > 0) {
        LK -= Y;
    } else {
        for (let Y = 0; Y < EI.length; Y++) {
            if (AY(EI[Y])) {
                break;
            }
        }
    }
}

function OY(Y) {
    H(Y);
    const C = L(Y, LH.x, LH.y) + TE;
    if (BK > C) {
        return;
    }
    const D = ZJ, E = YK, F = XJ, J = AJ, K = C - BK;
    if (Y.m & 8 && LH.ph > 0) {
        return;
    }
    I(Y, F, J);
    let M = LH.vx - UJ, N = LH.vy - RJ;
    const O = M * D + N * E;
    if (Y.m & 4) {
        if (-O > (LH.rp > 0 ? 300 : 900) * (LF(3) ? .5 : 1)) {
            QY(Y, F, J, 0, 0);
            return;
        }
    }
    LH.x += D * K;
    LH.y += E * K;
    LH.ra && IY(0);
    if (O >= 0) {
        return;
    }
    const P = Y.m & 1, Q = Y.m & 2;
    Y.f = 1;
    Y.m & EF && PY(Y, F, J);
    const S = (1 + (P ? 1.32 - NF(LH.sp / RE, 0, 1) * .72 : Q ? .12 : .44)) * -O;
    M += S * D;
    N += S * E;
    const V = -E, T = D, U = M * V + N * T, R = LE(KE(U), (Q ? 1.15 : .3) * S) * (U < 0 ? -1 : 1);
    M -= R * V;
    N -= R * T;
    LH.vx = M + UJ;
    LH.vy = N + RJ;
    HY();
    const W = -O;
    HB(W, Q ? 1 : P ? 2 : 0, Y.x + Y.y);
    HH = LE(26, HH + W * (P ? .008 : .0035));
    LC(F, J, NF(W * .012, 1, 14) | 0, 1, LE(W * .35, 420), MK(Y));
    if (P && W > 150) {
        BI++;
        CI = 1.6;
        const Y = (18 + W * .05) * WH * (LF(6) ? 2 : 1) | 0;
        RH += Y;
        if (LF(6)) {
            const Y = SE(B() * 7);
            PH[Y] = LE(SK(), PH[Y] + 2.4);
        }
        if (W > 620) {
            OC(F, J, "+" + Y, CF[2]);
            JC(F, J, 60 + LE(W * .2, 190), AL[6] + 30);
            KH = ME(KH, .045);
        }
    }
}

function PY(Y, B, C) {
    if (Y.lt) {
        return;
    }
    const D = Y.bk;
    Y.lt = 1;
    if (!D) {
        return;
    }
    D.l++;
    RH += 60 * WH | 0;
    JB(D.l / D.n);
    LC(B, C, 10, 0, 260, 48);
    if (D.l < D.n) {
        return;
    }
    D.l = 0;
    for (const Y of D.m) {
        Y.lt = 0;
    }
    const E = 420 * D.n * WH | 0;
    RH += E;
    UH += D.n * 2;
    for (let Y = 0; Y < 7; Y++) {
        PH[Y] = LE(SK(), PH[Y] + 5);
    }
    OC(D.x, D.y, "BANK +" + E, 48);
    JC(D.x, D.y, 320, 48);
    IH = ME(IH, .4);
    JH = 48;
    HH = LE(30, HH + 12);
    KH = ME(KH, .07);
    KB();
}

function QY(Y, B, C, D, E) {
    Y.k = 1;
    const F = MK(Y);
    HH = LE(32, HH + 10 - E);
    LC(B, C, 18, 2, 460, F);
    VY(B, C, Y, F);
    JC(B, C, 150, F);
    IB(E);
    const H = 60 * WH * (1 + E * .5) | 0;
    RH += H;
    BI++;
    CI = 1.6;
    if (!E) {
        OC(B, C, "+" + H, 45);
        KH = ME(KH, .05);
    }
    if (E > 3) {
        return;
    }
    const I = 150 * (LF(3) ? 2 : 1);
    for (const D of KK) {
        for (const F of D.o) {
            if (F === Y || F.k || F.kt || !(F.m & 4)) {
                continue;
            }
            const D = QE(F.x - B, F.y - C);
            if (D < I + F.r) {
                F.kt = .04 + D / I * .1;
                F.kd = E + 1;
            }
        }
    }
    LH.vx *= .9;
    LH.vy *= .9;
}

function SY(Y) {
    for (const B of DI) {
        for (const C of B.o) {
            if (C.kt && !C.k && !((C.kt -= Y) > 0)) {
                H(C);
                QY(C, VJ, TJ, 0, C.kd);
            }
        }
    }
}

function VY(Y, B, C, D) {
    for (let E = 0; E < 7; E++) {
        const E = SF(0, IE), F = SF(90, 420);
        KC(Y, B, OE(E) * F, NE(E) * F, SF(.5, 1.1), D, 4, SF(3, 8) * (C.t ? 1 : 1.4));
    }
}

function TY(Y) {
    const B = QE(LH.vx, LH.vy);
    KK = FY(LH.y - 420 - B * Y, LH.y + 420 + B * Y);
    let C = MH, D = NH;
    for (const Y of KK) {
        if (Y.z && LH.y >= Y.y && LH.y < Y.y + Y.h) {
            EY(Y, LH.x, LH.y);
            C += IK;
            D += JK;
            break;
        }
    }
    if (LH.te) {
        LH.te.t -= Y;
        LH.vx += C * Y;
        LH.vy += D * Y;
        LH.te.t <= 0 && KY();
    } else if (LH.ra) {} else {
        LH.vx += C * Y;
        LH.vy += D * Y;
    }
    HY();
    let E = QE(LH.vx, LH.vy);
    const F = 1400 * (LF(2) ? 1.5 : 1);
    if (E > F) {
        const B = ME(0, 1 - (E / F - 1) * (LF(2) ? .6 : 1.5) * Y);
        LH.vx *= B;
        LH.vy *= B;
        E *= B;
    }
    const H = NF(JE.ceil(E * Y / (TE * .55)), 1, 8), I = Y / H;
    for (let Y = 0; Y < H; Y++) {
        if (LH.ra) {
            if (!JY(I)) {
                LH.x += LH.vx * I;
                LH.y += LH.vy * I;
            }
        } else {
            LH.x += LH.vx * I;
            LH.y += LH.vy * I;
        }
        LH.te && LY();
        NY(I);
        MY(I);
    }
    DY(LH.y);
    if (LH.x < FK + TE) {
        LH.x = FK + TE;
        LH.vx < 0 && (LH.vx = .55 * -LH.vx + 60);
    }
    if (LH.x > HK - TE) {
        LH.x = HK - TE;
        LH.vx > 0 && (LH.vx = .55 * -LH.vx - 60);
    }
    LH.ph > 0 && (LH.ph -= Y);
    LH.rp > 0 && (LH.rp -= Y);
    if (LH.gt > 0) {
        LH.gt -= Y;
        if (LH.gt <= 0) {
            MH = 0;
            NH = UE;
        }
    }
    LH.sp = QE(LH.vx, LH.vy);
    LH.sp > 30 && (LH.a = PE(LH.vy, LH.vx));
}

function UY(Y) {
    for (let B = 0; B < 7; B++) {
        if (YI[B] > 0 && HF[B][0] === Y) {
            return HF[B][1];
        }
    }
    return 1;
}

function RY() {
    if (DH !== 1 || !LH.al) {
        return;
    }
    if (PH[QH] <= .5) {
        OK = QH;
        PK = .5;
        BC();
        return;
    }
    const Y = OI - LH.x, B = PI - LH.y, C = LE(1, AE / (QE(Y, B) || 1)), D = LH.x + Y * C, E = LH.y + B * C;
    if (!SI) {
        SI = 1;
        VI = 0;
    }
    QI = {
        x1: D,
        y1: E,
        x2: D,
        y2: E,
        e: DF[QH],
        c: QH,
        l: 0,
        u: 0,
        paid: 0,
        n: LF(8) ? 2 : 1
    };
    EI.push(QI);
    while (EI.length > 5 + (LF(7) ? 2 : 0)) {
        LH.ra === EI.shift() && IY(0);
    }
    LH.te && KY();
}

function WY() {
    const Y = QI;
    if (!Y) {
        return;
    }
    const B = OI - Y.x1, C = PI - Y.y1, D = QE(B, C);
    if (D < 1) {
        return;
    }
    let E = LE(D, 210);
    if (E > Y.paid) {
        const B = YF[Y.c] * QK(), C = (E - Y.paid) * B;
        if (PH[Y.c] >= C) {
            PH[Y.c] -= C;
            Y.paid = E;
        } else {
            E = Y.paid += PH[Y.c] / B;
            PH[Y.c] = 0;
            if (PK <= 0) {
                OK = Y.c;
                PK = .6;
                BC();
            }
        }
    }
    Y.x2 = Y.x1 + B / D * E;
    Y.y2 = Y.y1 + C / D * E;
    XY(Y);
}

function XY(Y) {
    for (const B of EI) {
        if (B === Y || B.u || B.e === Y.e) {
            continue;
        }
        const C = E(Y.x1, Y.y1, Y.x2, Y.y2, B.x1, B.y1, B.x2, B.y2);
        if (C) {
            Y.e = B.e = Y.e | B.e;
            JI.push({
                x: C[0],
                y: C[1],
                t: .55
            });
            AB();
        }
    }
}

function AY(Y) {
    const B = Y.x2 - Y.x1, C = Y.y2 - Y.y1, E = QE(B, C);
    if (Y.u || Y === QI || Y === LH.ra || E < 22) {
        return 0;
    }
    const F = D(Y.x1, Y.y1, Y.x2, Y.y2, LH.x, LH.y), H = Y.x1 + B * F, I = Y.y1 + C * F;
    let J = LH.x - H, K = LH.y - I;
    const L = QE(J, K);
    if (L > 22) {
        return 0;
    }
    const M = B / E, N = C / E;
    if (L < .001) {
        J = -N;
        K = M;
    } else {
        J /= L;
        K /= L;
    }
    ZY(Y, J, K, H, I, F, M, N, E);
    LK = .05;
    return 1;
}

function ZY(Y, B, C, D, E, F, H, I, J) {
    const K = Y.e, L = VK(J);
    let M = 1;
    if (K & 64) {
        const D = UY(6);
        if (K & 16) {
            LH.ph = .8 * D;
        } else {
            let E = 0;
            for (const B of EI) {
                if (B !== Y && !B.u && B.e & 64) {
                    E = B;
                    break;
                }
            }
            MC(LH.x, LH.y);
            if (E) {
                const Y = PE(E.y2 - E.y1, E.x2 - E.x1) - PE(I, H), B = OE(Y), C = NE(Y), D = LH.vx * B - LH.vy * C, F = LH.vx * C + LH.vy * B, J = 30 / (QE(D, F) || 1);
                LH.vx = D;
                LH.vy = F;
                LH.x = (E.x1 + E.x2) / 2 + D * J;
                LH.y = (E.y1 + E.y2) / 2 + F * J;
                E.u = 1;
                E.l = .12;
            } else {
                const Y = QE(LH.vx, LH.vy), E = 300 * D * L;
                if (Y < 60) {
                    LH.x += B * E;
                    LH.y += C * E;
                    LH.vx = B * 460;
                    LH.vy = C * 460;
                } else {
                    const B = E / Y;
                    LH.x += LH.vx * B;
                    LH.y += LH.vy * B;
                }
            }
            DY(LH.y);
            LH.x = NF(LH.x, FK + TE, HK - TE);
            LH.ph = .34 * D * L;
            LH.vx *= 1.07;
            LH.vy *= 1.07;
            MC(LH.x, LH.y);
        }
        SB();
    }
    if (K & 16) {
        LH.te && KY();
        LH.ra = Y;
        LH.rt = F;
        LH.rw = UY(4) > 1 ? 4 : 0;
        LH.rs = B * -I + C * H >= 0 ? 1 : -1;
        const D = ME(QE(LH.vx, LH.vy), 340 + 180 * L), E = LH.vx * H + LH.vy * I >= 0 ? 1 : -1;
        LH.vx = H * D * E;
        LH.vy = I * D * E;
        M = 0;
        PB(1);
    } else if (K & 8) {
        LH.ra && IY(0);
        const B = UY(3);
        LH.te = {
            x: Y.x1,
            y: Y.y1,
            l: ME(J * B, 30),
            t: (1.4 + L) * B
        };
        OB(1);
    }
    if (K & 32) {
        MH = B * UE;
        NH = C * UE;
        LH.gt = 2.9 * UY(5) * L;
        QB(C);
        LC(D, E, 10, 3, 150, CF[5]);
    }
    const N = K & 4;
    if (K & 2) {
        const Y = QE(LH.vx, LH.vy), B = N ? ME(Y * 1.34, 560 + 320 * L) : ME(Y * .99, 250 + 250 * L);
        LH.vx = H * B;
        LH.vy = I * B;
        MB(N);
    } else if (N) {
        const Y = LH.vx * B + LH.vy * C, D = (1.25 + .6 * L) * UY(2);
        LH.vx -= (1 + D) * Y * B;
        LH.vy -= (1 + D) * Y * C;
        const E = LH.vx * B + LH.vy * C, F = LE(300 + 300 * L, KE(Y) * 1.6);
        if (E < F) {
            LH.vx += (F - E) * B;
            LH.vy += (F - E) * C;
        }
        NB(KE(Y));
    } else if (!(K & 89)) {
        const Y = LH.vx * B + LH.vy * C;
        if (Y < 0) {
            LH.vx -= 1.55 * Y * B;
            LH.vy -= 1.55 * Y * C;
        }
    }
    if (K & 1) {
        const Y = UY(0);
        let F, H, I = QE(LH.vx, LH.vy);
        if (I < 50) {
            F = B;
            H = C;
            I = 0;
        } else {
            F = LH.vx / I;
            H = LH.vy / I;
            const Y = F * B + H * C;
            if (Y < 0) {
                F -= 2 * Y * B;
                H -= 2 * Y * C;
            }
        }
        const J = I * 1.5 + 440 * Y * L;
        LH.vx = F * J;
        LH.vy = H * J;
        LH.rp = .85;
        HH = LE(26, HH + 7);
        JC(D, E, 120 + 160 * L, CF[0]);
        LC(D, E, 26, 0, 220, CF[0], 420 * -F, 420 * -H);
        const K = (90 + 110 * L) * Y;
        for (const Y of KK) {
            for (const B of Y.o) {
                if (!B.k && !B.kt && B.m & 4 && QE(B.x - D, B.y - E) < K + B.r) {
                    B.kt = .02;
                    B.kd = 0;
                }
            }
        }
        LB(J);
    }
    HY();
    if (M && ! --Y.n) {
        Y.u = 1;
        Y.l = .22;
    }
    NC(Y, D, E);
    YB(K);
}

function YB(Y) {
    const B = SH;
    NK = 7;
    SH |= Y & 127;
    if (SH === B) {
        return;
    }
    let C = 0;
    for (let Y = 0; Y < 7; Y++) {
        SH & DF[Y] && C++;
    }
    if (!(C <= VH)) {
        VH = C;
        if (C === 7) {
            return BB();
        }
        if (C === 3 || C === 5) {
            for (let Y = 0; Y < 7; Y++) {
                PH[Y] = LE(SK(), PH[Y] + 6);
            }
            OC(LH.x, LH.y - 30, "SPECTRUM " + C + "/7", CF[C]);
            ZB();
        }
    }
}

function BB() {
    TH = 3.2;
    for (let Y = 0; Y < 7; Y++) {
        PH[Y] = LE(SK(), PH[Y] + 26);
    }
    WH = LE(12, WH + 1.5);
    RH += 1200 * WH | 0;
    IH = 1;
    JH = -1;
    HH = LE(32, HH + 14);
    OC(LH.x, LH.y - 46, "FULL SPECTRUM", -1);
    XB();
    SH = 0;
    VH = 0;
}

function CB(Y) {
    Y.g = 1;
    if (Y.t === 0) {
        BI++;
        CI = 1.4;
        UH += LF(9) ? 3 : 1;
        RH += 10 * WH * (1 + BI * .05) * (LF(9) ? 3 : 1) | 0;
        VB(BI);
    } else if (Y.t === 1) {
        UH += 15;
        RH += 500 * WH | 0;
        OC(Y.x, Y.y, "CROWN +15", 48);
        IH = ME(IH, .45);
        JH = 48;
        UB();
    } else if (Y.t === 2) {
        PH[Y.c] = LE(SK(), PH[Y.c] + 40);
        RB(Y.c);
        LC(Y.x, Y.y, 12, 3, 190, CF[Y.c]);
    } else if (Y.t === 3) {
        for (let Y = 0; Y < 7; Y++) {
            PH[Y] = SK();
        }
        IH = 1;
        JH = -1;
        OC(Y.x, Y.y, "PRISM WELL", -1);
        WB();
    } else {
        YI[Y.c] = HF[Y.c][2];
        OC(Y.x, Y.y, IF[Y.c], HF[Y.c][0] > 6 ? -1 : CF[HF[Y.c][0]]);
        YC();
    }
}

function DB() {
    if (BL) {
        BL.state === "suspended" && BL.resume();
        return;
    }
    const Y = window.AudioContext || window.webkitAudioContext;
    if (!Y) {
        return;
    }
    BL = new Y;
    WK = BL.createBiquadFilter();
    WK.type = "lowpass";
    WK.frequency.value = 2e4;
    TK = BL.createGain();
    TK.gain.value = LI.m ? 0 : .8;
    WK.connect(TK).connect(BL.destination);
    UK = BL.createGain();
    UK.gain.value = .85;
    UK.connect(WK);
    RK = BL.createGain();
    RK.gain.value = .5;
    RK.connect(WK);
    const B = BL.sampleRate * 2;
    XK = BL.createBuffer(1, B, BL.sampleRate);
    const C = XK.getChannelData(0);
    for (let Y = 0; Y < B; Y++) {
        C[Y] = VE() * 2 - 1;
    }
    ZK = BL.createOscillator();
    ZK.type = "sawtooth";
    ZK.frequency.value = 220;
    YL = BL.createBiquadFilter();
    YL.type = "bandpass";
    YL.frequency.value = 1400;
    YL.Q.value = 6;
    AK = BL.createGain();
    AK.gain.value = 0;
    ZK.connect(YL).connect(AK).connect(UK);
    ZK.start();
    DL = BL.currentTime + .1;
}

function EB(Y, B, C, D, E, F, H) {
    if (!JL()) {
        return;
    }
    const I = H || KL(), J = BL.createOscillator(), K = BL.createGain();
    J.type = Y;
    J.frequency.setValueAtTime(ME(8, B), I);
    C && C !== B && J.frequency.exponentialRampToValueAtTime(ME(8, C), I + D);
    K.gain.setValueAtTime(1e-4, I);
    K.gain.exponentialRampToValueAtTime(ME(1e-4, E), I + .008);
    K.gain.exponentialRampToValueAtTime(1e-4, I + D);
    J.connect(K).connect(F || UK);
    J.start(I);
    J.stop(I + D + .02);
    CL++;
}

function FB(Y, B, C, D, E, F, H, I) {
    if (!JL()) {
        return;
    }
    const J = I || KL(), K = BL.createBufferSource();
    K.buffer = XK;
    K.loop = 1;
    K.playbackRate.value = .7 + VE() * .6;
    const L = BL.createBiquadFilter();
    L.type = C;
    L.frequency.setValueAtTime(ME(20, D), J);
    E !== D && L.frequency.exponentialRampToValueAtTime(ME(20, E), J + Y);
    L.Q.value = F || 1;
    const M = BL.createGain();
    M.gain.setValueAtTime(1e-4, J);
    M.gain.exponentialRampToValueAtTime(ME(1e-4, B), J + .005);
    M.gain.exponentialRampToValueAtTime(1e-4, J + Y);
    K.connect(L).connect(M).connect(H || UK);
    K.start(J);
    K.stop(J + Y + .02);
    CL++;
}

function HB(Y, B, C) {
    if (Y < 60) {
        return;
    }
    const D = NF(Y / 2200, .04, .55);
    FB(.03 + D * .12, D * .7, "lowpass", 300 + Y * 1.6, 140, 1);
    Y > 380 && EB("triangle", 120 + Y * .05, 44, .1 + D * .12, D * .8);
    if (B > 1) {
        const Y = HL[AH % 4], B = KE(C | 0), E = 64 + Y[(B >> 5) % Y.length] + 12 * (B >> 9 & 1);
        EB("sine", FL(E), FL(E) * 1.5, .17, D * .55);
        EB("triangle", FL(E + 12), 0, .1, D * .3);
    } else {
        B && EB("sine", 90, 42, .25, D * .5);
    }
}

function IB(Y) {
    const B = 1 / (1 + Y * .5);
    FB(.3 * B, .45 * B, "bandpass", 2600 * (1 + Y * .3), 300, 1.4);
    EB("triangle", 160 * (1 + Y * .22), 38, .34 * B, .5 * B);
    FB(.09, .3 * B, "highpass", 4e3, 9e3, 1);
}

function JB(Y) {
    const B = 76 + Y * 12 | 0;
    EB("square", FL(B), FL(B + 7), .07, .13);
    EB("triangle", FL(B + 12), 0, .13, .1);
}

function KB() {
    TB(72, [ 0, 4, 7, 12, 16, 19 ], .45, .13, "square", .04, 12);
    FB(.7, .24, "bandpass", 700, 6e3, .8);
    EB("sine", 90, 300, .4, .3);
}

function LB(Y) {
    const B = NF(Y / 2600, .16, .5);
    EB("sawtooth", 150, 780 + Y * .2, .22, B * .5);
    FB(.34, B * .7, "bandpass", 700, 4200, 1.1);
    EB("sine", 70, 34, .3, B * .8);
}

function MB(Y) {
    EB("square", Y ? 700 : 940, Y ? 2e3 : 1680, .07, .16);
    FB(.05, .12, "highpass", 2600, 5200, 1);
    Y && EB("sine", 320, 900, .16, .2);
}

function NB(Y) {
    const B = NF(Y / 1400, .12, .4);
    EB("sine", 180 + Y * .1, 700 + Y * .3, .1, B);
    EB("sine", 700 + Y * .3, 210, .22, B * .8);
}

function OB(Y) {
    if (Y) {
        EB("triangle", 240, 720, 1.1, .13);
        FB(.06, .1, "bandpass", 900, 2400, 3);
    } else {
        EB("sawtooth", 900, 190, .13, .26);
        FB(.1, .22, "bandpass", 1800, 500, 2);
    }
}

function PB(Y) {
    if (BL) {
        AK && AK.gain.setTargetAtTime(Y && !LI.m ? .12 : 0, KL(), .03);
        Y && EB("square", 1200, 2400, .05, .1);
    }
}

function QB(Y) {
    const B = Y < 0;
    EB("sine", B ? 120 : 460, B ? 460 : 110, .5, .3);
    EB("triangle", B ? 240 : 700, B ? 700 : 200, .38, .12);
    FB(.4, .1, "lowpass", 900, 200, 1);
}

function SB() {
    EB("sawtooth", 2600, 260, .1, .2);
    FB(.08, .18, "highpass", 7e3, 1400, 2);
    EB("sine", 90, 240, .22, .26);
}

function VB(Y) {
    const B = 74 + LE(Y, 14) * 2;
    EB("square", FL(B), 0, .06, .1);
    EB("sine", FL(B + 12), 0, .1, .12);
}

function TB(Y, B, C, D, E, F, H) {
    if (!BL) {
        return;
    }
    const I = KL();
    B.forEach((B, J) => EB(E, FL(Y + B), H ? FL(Y + B + H) : 0, C, D, 0, I + J * F));
}

function UB() {
    TB(72, LL, .5, .12, "triangle", .045);
}

function RB(Y) {
    EB("triangle", FL(64 + Y * 2), FL(76 + Y * 2), .16, .16);
}

function WB() {
    TB(60, HL[1], .5, .13, "sine", .06, 12);
    FB(.9, .16, "bandpass", 800, 6e3, .8);
}

function XB() {
    TB(60, HL[1], .7, .12, "sawtooth", .05, 12);
    TB(84, LL, 1.5, .1, "sine", 0);
    BL && FB(1.2, .2, "bandpass", 400, 8e3, .7, 0, KL() + .3);
}

function AB() {
    EB("square", 1500, 2600, .05, .09);
    EB("sine", 900, 1800, .1, .09);
}

function ZB() {
    EB("triangle", FL(76), FL(83), .18, .12);
}

function YC() {
    TB(62, [ 0, 5, 10 ], .12, .1, "square", .06, 2);
}

function BC() {
    EB("sine", 150, 70, .12, .18);
    FB(.06, .07, "lowpass", 500, 200, 1);
}

function CC(Y) {
    EB("sine", 70 + Y * 40, 50, .18, .12 + Y * .18);
}

function DC() {
    TB(72, [ 0, -3, -6, -9, -12, -15, -18 ], .8, .1, "sawtooth", .04, -30);
    FB(1, .3, "lowpass", 3e3, 90, 1);
    EB("sine", 120, 30, 1.1, .3);
}

function EC(Y) {
    EB("square", Y ? 900 : 620, Y ? 1200 : 500, .04, .07);
}

function FC() {
    TB(48, [ 0, 7, 12, 16, 19 ], 1.6, .1, "triangle", 0);
    FB(1.4, .22, "bandpass", 300, 5e3, .6);
}

function HC() {
    CL = 0;
    if (!BL) {
        return;
    }
    const Y = KL(), B = NF(LH.sp / RE, 0, 1), C = DH === 1 && LH.al, D = (B, C, D) => B.setTargetAtTime(C, Y, D);
    D(AK.gain, LH.ra && !LI.m && C ? .1 + B * .12 : 0, .04);
    D(YL.frequency, 700 + LH.sp * 1.4, .04);
    D(ZK.frequency, 90 + LH.sp * .22, .04);
    D(WK.frequency, FH > .05 ? 480 : DH === 2 ? 700 : 2e4, .1);
    D(RK.gain, LI.m ? 0 : DH === 1 ? .5 : .34, .2);
    C ? IC() : DL = Y + .1;
}

function IC() {
    const Y = 40 + AH * 5 % 11, B = HL[AH % 4], C = IL[AH % 3], D = NF(LH.sp / 1400, 0, 1.5) + (TH > 0 ? .6 : 0), E = 15 / ((88 + AH * 7) * (FH > .5 ? .5 : 1)), F = ML[AH], H = NL[AH], I = KL();
    for (let J = 8; DL < I + .16 && J--; ) {
        const J = ME(DL, I), K = EL, L = K & 15, M = K >> 4, N = (M * 2 + (L > 7 ? 1 : 0)) % B.length, O = Y - 12 + B[N];
        if (F >> L & 1) {
            EB("sine", 130, 42, .17, .5, RK, J);
            FB(.03, .12, "lowpass", 900, 200, 1, RK, J);
        }
        H >> L & 1 && EB(C, FL(O), 0, .2, .3, RK, J);
        if (!L) {
            for (const C of [ 0, 3 + (B[2] > 3 ? 1 : 0), 7 ]) {
                EB("triangle", FL(Y + B[N] + C), 0, E * 15, .1, RK, J);
            }
        }
        D > .25 && L & 1 && FB(.03, .04 + D * .03, "highpass", 7e3, 6500, 1, RK, J);
        D > .5 && L % 4 == 2 && FB(.09, .13, "bandpass", 1900, 900, 1.2, RK, J);
        if (D > .8 && !(L & 1)) {
            const D = FL(Y + 12 + B[(K * 3 + M) % B.length]);
            EB(C, D, D, .1, .09, RK, J);
        }
        EL++;
        DL = J + E;
    }
}

function JC(Y, B, C, D) {
    KI.length > 26 && KI.shift();
    KI.push({
        x: Y,
        y: B,
        r: 0,
        R: C,
        h: D,
        t: 1
    });
}

function KC(Y, B, C, D, E, F, H, I) {
    FI.length > 420 && FI.shift();
    FI.push({
        x: Y,
        y: B,
        vx: C,
        vy: D,
        l: E,
        L: E,
        h: F,
        k: H,
        s: I,
        a: SF(0, IE),
        w: SF(-9, 9)
    });
}

function LC(Y, B, C, D, E, F, H, I) {
    const J = F === void 0 ? CF[QH] : F, K = D === 1 ? LI.e[3] : D;
    for (let D = 0; D < C; D++) {
        const C = SF(0, IE), D = SF(.3, 1) * E;
        KC(Y, B, (H || 0) + OE(C) * D, (I || 0) + NE(C) * D, SF(.18, .55), J + SF(-18, 18), K, SF(1.4, 3.6));
    }
}

function MC(Y, B) {
    KC(Y, B, 0, 0, .4, CF[6], 2, 8);
    LC(Y, B, 12, 0, 320, CF[6]);
}

function NC(Y, B, C) {
    for (let D = 0; D < 7; D++) {
        Y.e & DF[D] && LC(B, C, 5, 0, 240, CF[D]);
    }
    KC(B, C, 0, 0, .3, CF[Y.c], 2, 6);
}

function OC(Y, B, C, D) {
    II.push({
        x: Y,
        y: B,
        t: C,
        h: D,
        l: 1.15
    });
}

function PC(Y) {
    for (let B = FI.length; B--; ) {
        const C = FI[B];
        if ((C.l -= Y) <= 0) {
            FI.splice(B, 1);
        } else {
            C.x += C.vx * Y;
            C.y += C.vy * Y;
            if (C.k !== 2) {
                C.vy += 400 * Y;
                C.vx *= .985;
                C.vy *= .985;
            }
            C.k === 4 && (C.a += C.w * Y);
        }
    }
    for (let B = KI.length; B--; ) {
        const C = KI[B];
        C.r += (C.R - C.r) * LE(1, 9 * Y);
        (C.t -= Y * 2.2) <= 0 && KI.splice(B, 1);
    }
    for (let B = II.length; B--; ) {
        const C = II[B];
        C.y -= 46 * Y;
        (C.l -= Y) <= 0 && II.splice(B, 1);
    }
    for (let B = JI.length; B--; ) {
        (JI[B].t -= Y) <= 0 && JI.splice(B, 1);
    }
}

function QC(Y) {
    const B = HJ;
    OH.x = PF(OH.x, B ? B.cx : LH.x + NF(LH.vx * .12, -180, 180), B ? 3.5 : 7.5, Y);
    OH.y = PF(OH.y, B ? B.cy : LH.y + NF(LH.vy * .24, -117.8, 155), B ? 3.5 : 7.5, Y);
    OH.z = PF(OH.z, B ? .82 : 1 / (1 + LH.sp / 5200), 4, Y);
    CH = LE(BH / 620, YH / 846) * OH.z;
    const C = ME(0, 611 - YH / 2 / CH);
    OH.x = NF(OH.x, -C, C);
    HH = ME(0, HH - HH * 7 * Y - 4 * Y);
    const D = LE(HH, 24);
    ZL = SF(-D, D);
    YM = SF(-D, D);
}

function SC(Y) {
    const B = HM(AH);
    for (let C = 0; C < 8; C++) {
        AL[C] = C % 3 || C > 6 ? PF(AL[C], B[C], 1.6, Y) : FM(AL[C], B[C], 1.6, Y);
    }
}

function VC() {
    const Y = ZF.createLinearGradient(0, 0, 0, BH);
    Y.addColorStop(0, WF(AL[0] | 0, AL[1] | 0, AL[2] | 0));
    Y.addColorStop(1, WF(AL[3] | 0, AL[4] | 0, AL[5] | 0));
    ZF.fillStyle = Y;
    ZF.fillRect(0, 0, YH, BH);
    const B = IM[AH];
    ZF.lineWidth = ME(1, 2.4 * CH);
    for (let Y = 0; Y < 2; Y++) {
        const D = Y ? .4 : .16, E = Y ? 330 : 560, F = Y ? .085 : .045, H = OH.x * D, I = OH.y * D, J = YH / 2 / CH, K = BH / 2 / CH, L = SE((H + J) / E), M = SE((I + K) / E);
        for (let Y = SE((I - K) / E); Y <= M; Y++) {
            for (let D = SE((H - J) / E); D <= L; D++) {
                const J = C(D * 31 + B[0], Y * 17);
                if (J < .5) {
                    continue;
                }
                const K = (D + .15 + C(D, Y) * .7) * E, L = (Y + .15 + C(Y, D + 9) * .7) * E, M = AL[6] + 60 * (J - .5) | 0;
                ZF.fillStyle = WF(M, 60, AL[7] | 0, F);
                ZF.strokeStyle = WF(M, 70, AL[7] | 0, F * 2);
                TC(B, YH / 2 + (K - H) * CH, BH / 2 + (L - I) * CH, E * (.16 + J * .3) * CH, J);
            }
        }
    }
}

function TC(Y, B, D, E, F) {
    const H = Y[1], I = Y[2];
    OL();
    if (Y[0]) {
        if (Y[0] < 2) {
            for (let Y = 0; Y < H; Y++) {
                const J = Y / H * IE + F * 4, K = E * (I < 0 ? .6 + C(Y, F * 99) * .5 : Y & 1 ? I / 10 : 1);
                XL(Y, B + OE(J) * K, D + NE(J) * K);
            }
            ZF.closePath();
            ZF.fill();
        } else {
            const Y = I < 0 ? 2.4 : IE, C = KE(I);
            for (let C = 1; C <= H; C++) {
                OL();
                SL(B, D, E * (1 - C * .22), F * IE, F * IE + Y);
                ZF.stroke();
            }
            OL();
            for (let Y = 0; Y < C; Y++) {
                const H = Y / C * IE + EH * .06 * (F > .7 ? -1 : 1);
                VL(B + OE(H) * E * .5, D + NE(H) * E * .5);
                TL(B + OE(H) * E, D + NE(H) * E);
            }
            ZF.stroke();
        }
    } else {
        const Y = 4 + (F * 4 | 0), H = D + .34 * (E *= .58);
        for (let D = 0; D < Y; D++) {
            const I = D / (Y - 1) - .5, J = E * (.3 + .34 * OE(I * 2.6) + C(D * 7, F * 400) * .16), K = B + I * E * 1.7, L = H - J * (.72 + C(D, F * 90) * .5);
            VL(K + J, L);
            SL(K, L, J);
        }
        VL(B - E * .95, H);
        ZF.roundRect(B - E * .95, H - E * .34, E * 1.9, E * .34, E * .17);
        ZF.fill();
    }
}

function UC(Y) {
    const B = Y.m, C = B & EF ? Y.lt ? 6 : 5 : B & 2 ? 0 : B & 4 ? 1 : B & 8 ? 2 : B & 32 ? 3 : B & 16 ? 4 : -1;
    if (C < 0) {
        return B & 1 ? [ WF(AL[6] + 30 | 0, 92, 48), WF(AL[6] + 40 | 0, 100, 80) ] : [ WF(AL[6] | 0, 52, 19), WF(AL[6] | 0, 84, ME(58, AL[7]) | 0) ];
    }
    const D = JM[C];
    return [ WF(D[0], D[1], D[2]), WF(D[0], D[3], D[4]) ];
}

function RC() {
    const Y = EM(0) - 200, B = EM(BH) + 200, C = ME(2, 13 * CH), D = ME(1, 5 * CH), E = WF(AL[6] | 0, 34, ME(4, AL[2] * .42) | 0);
    ZF.lineCap = "round";
    for (const F of DI) {
        if (F.y + F.h < Y || F.y > B) {
            continue;
        }
        const I = CM(F.y), J = CM(F.y + F.h);
        ZF.fillStyle = E;
        for (const Y of [ -9e3, 9e3 ]) {
            OL();
            VL(Y, I);
            TL(BM(Y < 0 ? F.pl : F.pr), I);
            TL(BM(Y < 0 ? F.l : F.r), J);
            TL(Y, J);
            ZF.fill();
        }
        F.z && WC(F);
        for (const Y of F.o) {
            if (Y.k) {
                continue;
            }
            H(Y);
            const B = UC(Y), E = BM(VJ), F = CM(TJ), I = Y.f > 0 ? Y.f = ME(0, Y.f - .05) : 0, J = Y.kt > 0 ? 1 : 0;
            if (I || J) {
                ZF.save();
                ZF.translate(E, F);
                ZF.scale(1 + I * .3, 1 + I * .3);
                ZF.translate(-E, -F);
                J && (ZF.globalAlpha = .5 + VE() * .5);
            }
            if (Y.t) {
                const E = OE(WJ) * Y.r, F = NE(WJ) * Y.r;
                UL(BM(VJ - E), CM(TJ - F), BM(VJ + E), CM(TJ + F), C, B[0]);
                PL(D, B[1]);
                if (Y.m & 4) {
                    ZF.setLineDash([ 6 * CH, 7 * CH ]);
                    PL(ME(1, 2 * CH), "hsl(48 100% 80% / .85)");
                    ZF.setLineDash([]);
                }
                I && PL(ME(1, (2 + I * 5) * CH), WF(AL[6] + 30 | 0, 100, 85, I));
            } else {
                RL(E, F, Y.r * CH, B[0], B[1], ME(1, (2.5 + I * 4) * CH));
                Y.m & 1 && RL(E, F, Y.r * CH * .55, 0, B[1], ME(1, 1.6 * CH));
                I && RL(E, F, (Y.r + 6 + I * 22) * CH, 0, WF(AL[6] + 30 | 0, 100, 80, I), ME(1, 2 * CH));
            }
            if (I || J) {
                ZF.globalAlpha = 1;
                ZF.restore();
            }
        }
        for (const Y of F.bk) {
            if (Y.l) {
                const B = BM(Y.x), C = CM(Y.y) - 46 * CH;
                for (let D = 0; D < Y.n; D++) {
                    RL(B + 13 * (D - (Y.n - 1) / 2) * CH, C, 4 * CH, D < Y.l ? "hsl(48 100% 68%)" : "hsl(48 30% 32%)");
                }
            }
        }
        for (const Y of F.i) {
            Y.g || XC(Y);
        }
    }
}

function WC(Y) {
    const B = ME(Y.y, EM(0) - 100), D = LE(Y.y + Y.h, EM(BH) + 100) - B;
    if (D <= 0) {
        return;
    }
    const E = Y.z, F = E === 1 ? 190 : E === 2 ? 288 : E === 3 ? 24 : E === 4 ? 262 : E === 5 ? 150 : 186, H = EH * (E === 3 ? 60 : E === 6 ? 320 : 200) * (E === 1 || E === 4 ? -1 : 1);
    ZF.lineCap = "round";
    for (let E = 0; E < 24; E++) {
        const I = C(E * 13, Y.y | 0), J = OF(Y.l + 30, Y.r - 30, C(E, 7)), K = B + ((I * D + H) % D + D) % D;
        EY(Y, J, K);
        const L = QE(IK, JK);
        if (L < 1) {
            continue;
        }
        const M = LE(64, L * .05);
        UL(BM(J), CM(K), BM(J + IK / L * M), CM(K + JK / L * M), ME(1, 2.2 * CH), WF(F, 90, 72, .16 + I * .18));
    }
}

function XC(Y) {
    const B = BM(Y.x), C = CM(Y.y), D = CH, E = 1 + NE(EH * 3 + Y.x * .02) * .12, F = Y.t;
    if (!F) {
        RL(B, C, 9 * D * E, VM, "hsl(38 100% 84%)", ME(1, 2.2 * D));
        return;
    }
    if (F === 3) {
        for (let Y = 0; Y < 7; Y++) {
            const F = Y / 7 * IE + EH * .6;
            OL();
            SL(B, C, 24 * D * E, F, F + .8);
            PL(7 * D, XF(Y, 62));
        }
        RL(B, C, 13 * D * E, "#fff");
        return;
    }
    const H = F === 1, I = F === 2, J = I ? Y.c : HF[Y.c][0], K = H ? 10 : I ? 6 : 3, L = (H ? 16 : I ? 12 : 15) * D * E;
    ZF.save();
    ZF.translate(B, C);
    ZF.rotate(EH * (H ? .8 : I ? 1.4 : -1) + Y.c);
    WL(K, H ? VM : I ? XF(J, 58) : J > 6 ? "#fff" : XF(J, 60), I ? XF(J, 86) : "#fff", ME(1, 2 * D), Y => {
        const B = Y / K * IE, C = L * (H && Y & 1 ? .44 : 1);
        XL(Y, OE(B) * C, NE(B) * C);
    });
    ZF.restore();
}

function AC(Y, B) {
    const C = [];
    for (let B = 0; B < 7; B++) {
        Y.e & DF[B] && C.push(B);
    }
    if (C.length < 2) {
        return XF(C[0] | 0, 62, B);
    }
    const D = ZF.createLinearGradient(BM(Y.x1), CM(Y.y1), BM(Y.x2), CM(Y.y2));
    C.forEach((Y, E) => D.addColorStop(E / (C.length - 1), XF(Y, 64, B)));
    return D;
}

function ZC() {
    ZF.lineCap = "round";
    for (const Y of EI) {
        const B = Y.u ? NF(Y.l / .22, 0, 1) : 1, C = BM(Y.x1), D = CM(Y.y1), E = BM(Y.x2), F = CM(Y.y2);
        UL(C, D, E, F, ME(4, 22 * CH), AC(Y, B * .18));
        PL(ME(3, 15 * CH), "hsl(266 55% 5% / " + B * .8 + ")");
        PL(ME(2, 10 * CH), AC(Y, B));
        PL(ME(1, 4 * CH), "hsl(0 0% 100% / " + B * .55 + ")");
        if (!Y.u) {
            RL(C, D, (4.5 + NE(EH * 4 + Y.x1) * .8) * CH, 0, XF(Y.c, 80, .9), ME(1, 2 * CH));
            const B = E - C, H = F - D, I = QE(B, H);
            if (I > 16) {
                const C = B / I, D = H / I, J = 9 * CH;
                WL(3, XF(Y.c, 78, .95), 0, 0, Y => {
                    const B = [ 0, 2.6, -2.6 ][Y], H = OE(B) * J, I = NE(B) * J;
                    XL(Y, E + H * C - I * D, F + I * C + H * D);
                });
            }
            for (let B = 1; B < Y.n; B++) {
                RL((C + E) / 2 + B * 8 * CH, (D + F) / 2, 2.6 * CH, "#fff");
            }
        }
        if (Y === LH.ra) {
            ZF.setLineDash([ 4 * CH, 9 * CH ]);
            ZF.lineDashOffset = 90 * -EH * CH;
            PL(ME(1, 3 * CH), "hsl(190 100% 88% / .9)");
            ZF.setLineDash([]);
        }
    }
    for (const Y of JI) {
        const B = Y.t / .55;
        RL(BM(Y.x), CM(Y.y), (6 + 26 * (1 - B)) * CH, 0, "hsl(0 0% 100% / " + B + ")", ME(1, 3 * CH));
    }
    if (LH.te) {
        const Y = BM(LH.te.x), B = CM(LH.te.y);
        RL(Y, B, LH.te.l * CH, 0, XF(3, 60, .17), ME(1, 1.4 * CH));
        UL(Y, B, BM(LH.x), CM(LH.y), ME(1, 3.4 * CH), XF(3, 70));
        RL(Y, B, 8 * CH, XF(3, 62));
        RL(Y, B, (12 + NE(EH * 9) * 3) * CH, 0, XF(3, 80, .8), ME(1, 2 * CH));
    }
}

function YD() {
    ZF.lineCap = "round";
    for (const Y of KI) {
        const B = NF(Y.t, 0, 1);
        RL(BM(Y.x), CM(Y.y), Y.r * CH, 0, WF(Y.h | 0, 100, 78, B * .7), ME(1, 5 * B * CH));
    }
    for (const Y of FI) {
        const B = NF(Y.l / Y.L, 0, 1), C = BM(Y.x), D = CM(Y.y);
        if (Y.k === 4) {
            ZF.save();
            ZF.translate(C, D);
            ZF.rotate(Y.a);
            const E = Y.s * CH * (.4 + B * .6);
            WL(4, WF(Y.h | 0, 70, 38 + B * 26, B), WF(Y.h | 0, 90, 78, B), ME(1, 1.2 * CH), Y => XL(Y, [ -1, .8, 1.2, -.6 ][Y] * E, [ -.7, -1.1, .9, 1 ][Y] * E));
            ZF.restore();
            continue;
        }
        Y.k === 2 ? RL(C, D, (1 - B) * Y.s * 9 * CH, 0, WF(Y.h | 0, 100, 75, B), ME(1, 3 * B * CH)) : UL(C, D, C - Y.vx * (Y.k ? .02 : .012) * CH, D - Y.vy * (Y.k ? .02 : .012) * CH, ME(1, Y.s * (Y.k ? 1 : B) * CH), WF(Y.h | 0, Y.k ? 70 : 100, Y.k ? 55 : 68, B));
    }
    for (const Y of II) {
        const B = NF(Y.l, 0, 1);
        FD(Y.t, BM(Y.x), CM(Y.y), 17 * CH / TM, Y.h < 0 ? "hsl(0 0% 100% / " + B + ")" : WF(Y.h | 0, 100, 70, B), 1);
    }
}

function BD() {
    const Y = HI.length ? HI[HI.length - 1] : 0;
    if (!Y || QE(LH.x - Y.x, LH.y - Y.y) > 7) {
        HI.push({
            x: LH.x,
            y: LH.y,
            h: TH > 0 ? -1 : CF[QH]
        });
        while (HI.length > 26 + NF(LH.sp / 34, 0, 34)) {
            HI.shift();
        }
    }
}

function CD() {
    const Y = HI.length, B = LI.e[2];
    ZF.lineCap = "round";
    for (let C = 1; C < Y; C++) {
        if (B === 1 && C % 2) {
            continue;
        }
        const D = C / Y, E = HI[C - 1], F = HI[C];
        UL(BM(E.x), CM(E.y), BM(F.x), CM(F.y), ME(1, 15 * D * CH * (B === 2 ? .6 + VE() * .8 : 1)), WF((F.h < 0 ? (EH * 400 + C * 26) % 360 : F.h) | 0, 100, 60 + D * 18, D * .8));
    }
}

function DD(Y, B, C, D) {
    const E = C ? "#fff" : Y === 1 ? "hsl(268 40% 12%)" : Y === 2 ? "hsl(190 100% 72%)" : "hsl(300 40% 96%)", F = C ? XF(SE(EH * 6) % 7, 70) : Y === 2 ? "hsl(300 100% 70%)" : "hsl(280 45% 62%)", H = NE(EH * 13), I = SE(EH * 5);
    ZF.lineCap = "round";
    OL();
    VL(-13, 0);
    for (let Y = 1; Y < 5; Y++) {
        TL(-13 - Y * 6, NE(EH * 9 - Y * .8) * (2 + Y * 1.6));
    }
    PL(5, XF((I + 2) % 7, 65));
    PL(2.4, XF((I + 5) % 7, 80));
    for (let Y = 0; Y < 4; Y++) {
        const B = 15 * (Y >> 1) - 8, C = H * (Y & 1 ? 1 : -1) * (Y > 1 ? -1 : 1);
        OL();
        VL(B, 6);
        TL(B + C * 5, 15);
        TL(B + C * 8 + 2, 20);
        PL(3, E);
    }
    OL();
    ZF.ellipse(0, 0, 17, 11, 0, 0, IE);
    QL(E);
    PL(2, F);
    OL();
    VL(8, -6);
    ZF.quadraticCurveTo(20, -14, 25, -12);
    ZF.quadraticCurveTo(31, -11, 30, -5);
    TL(20, -1);
    ZF.closePath();
    QL(E);
    PL(2, F);
    for (let Y = 0; Y < 4; Y++) {
        OL();
        VL(6 + Y * 4, -7 - Y);
        ZF.quadraticCurveTo(2 + Y * 4, -16 - Y * 1.5, 9 + Y * 4, -18 - Y);
        PL(3.4, XF((I + Y) % 7, 66));
    }
    ZF.save();
    ZF.translate(26, -12);
    ZF.rotate(-.62);
    const J = D === 1 ? 24 : 15, K = ZF.createLinearGradient(0, 0, 0, -J);
    K.addColorStop(0, XF(0, 65));
    K.addColorStop(1, XF(B, 82));
    WL(3, K, "#fff", 1.2, Y => XL(Y, [ -3, 3, 0 ][Y], [ 0, 0, -J ][Y]));
    if (!D) {
        for (let Y = 0; Y < 4; Y++) {
            const B = Y / 4;
            UL(B * 6 * .5 - 3, -J * B, 3 - B * 6 * .5, -J * B - 2, 1.1, XF((I + Y) % 7, 84));
        }
    }
    D === 2 && WL(10, "#fff", XF(B, 88), 1, Y => {
        const B = Y / 10 * IE + EH * 2, C = Y & 1 ? 2.4 : 6;
        XL(Y, OE(B) * C, -J + NE(B) * C);
    });
    ZF.restore();
    RL(26, -8, 1.7, DH === 1 && LH.st > BF ? "#f44" : "#2a1836");
}

function ED() {
    const Y = CH, B = BM(LH.x), C = CM(LH.y);
    ZF.save();
    ZF.translate(B, C);
    ZF.rotate(LH.a);
    ZF.scale(Y, Y);
    DD(LI.e[0], QH, TH > 0 || LH.ph > 0, LI.e[1]);
    ZF.restore();
    RL(B, C, 24 * Y, 0, LH.rp > 0 ? "hsl(6 100% 62%)" : XF(QH, 70, .55), ME(1, (LH.rp > 0 ? 3 : 2) * Y));
    if (LH.st > BF) {
        const D = (LH.st - BF) / 1.5;
        OL();
        SL(B, C, (33 + NE(EH * 22) * 4) * Y, -HE / 2, -HE / 2 + IE * (1 - D));
        PL(ME(2, 4 * Y), "hsl(" + (30 - D * 30) + " 100% 60%)");
    }
}

function FD(Y, B, C, D, E, F, H) {
    ZF.font = (F ? "bold " : "") + (D * TM | 0) + "px monospace";
    ZF.textAlign = H || "center";
    ZF.textBaseline = "middle";
    ZF.fillStyle = E;
    ZF.fillText(Y, B, C);
}

function HD(Y, B, C, D, E, F, H) {
    const I = WM(Y, B, C, D);
    UM.push({
        x: Y,
        y: B,
        w: C,
        h: D,
        fn: F
    });
    RM(Y - C / 2, B - D / 2, C, D, 8 * TM);
    QL(I ? H || SM : QM);
    PL(2 * TM, I ? NM : SM);
    FD(E, Y, B + TM, 15, I ? NM : OM, 1);
}

function ID() {
    for (const Y of UM) {
        if (WM(Y.x, Y.y, Y.w, Y.h)) {
            EC(1);
            Y.fn();
            return 1;
        }
    }
    return 0;
}

function JD() {
    const Y = 30 * TM, B = 240 * TM, C = ME(LH.y, 0);
    RL(Y, Y, 9 * TM, VM);
    FD(UH + (LI.c ? " (" + (LI.c + UH) + ")" : ""), Y + 16 * TM, Y, 16, VM, 1, "left");
    FD(RH | 0, YH - Y, Y, 19, NM, 1, "right");
    WH > 1.05 && FD("x" + WH.toFixed(1), YH - Y, Y + 22 * TM, 15, XF(2, 70), 1, "right");
    if (BI > 3) {
        const B = 1 + LE(BI, 30) * .02, C = NF(CI / 1.6, 0, 1);
        FD(BI + " CHAIN", YH - Y, Y + 46 * TM, 15 * B, WF(48, 100, 60 + BI, C), 1, "right");
    }
    ZF.fillStyle = PM;
    ZF.fillRect(YH / 2 - B / 2, Y + 12 * TM, B, 4 * TM);
    ZF.fillStyle = WF(AL[6] | 0, 90, 70);
    ZF.fillRect(YH / 2 - B / 2, Y + 12 * TM, B * NF(C % FF / FF, 0, 1), 4 * TM);
    FD(TI[AH][0] + (DK(LH.y) ? " +" + DK(LH.y) : ""), YH / 2, 18 * TM, 14, OM, 1);
    FD((C / 10 | 0) + "m", YH / 2 + B / 2 + 24 * TM, Y + 14 * TM, 13, PM, 0, "left");
    KD();
    let D = 0;
    for (let B = 0; B < 7; B++) {
        YI[B] > 0 && FD(IF[B] + " " + YI[B].toFixed(1), YH - Y, Y + (30 + 16 * D++) * TM, 12, HF[B][0] > 6 ? NM : XF(HF[B][0], 70), 1, "right");
    }
    for (let B = 0, C = 0; B < KF; B++) {
        LF(B) && FD(JF[B * 2], Y - 12 * TM, (58 + 15 * C++) * TM, 11, XF(B % 7, 72), 1, "left");
    }
    ZH > 0 && FD(TI[AH][0], YH / 2, BH * .3, 42, "hsl(0 0% 100% / " + NF(ZH, 0, 1) * NF(3.2 - ZH, 0, 1) + ")", 1);
    if (!LI.t) {
        const Y = [ "DRAG TO DRAW A RAINBOW RAIL", "PRESS 1-7 OR SCROLL TO CHANGE COLOUR", "PIGMENT IS FINITE - GRAB SHARDS" ][SI];
        Y && FD(Y, YH / 2, BH - 118 * TM, 15, OM, 1);
    }
    if (FH > .05) {
        ZF.fillStyle = "hsl(275 60% 60% / " + FH * .1 + ")";
        ZF.fillRect(0, 0, YH, BH);
        FD("FOCUS VAULT", YH / 2, BH - 96 * TM, 16, OM, 1);
    }
}

function KD() {
    const Y = 44 * TM, B = YH / 2 - 25 * Y / 7, C = BH - 40 * TM, D = 26 * TM;
    for (let E = 0; E < 7; E++) {
        const F = B + E * Y * 50 / 44, H = E === QH, I = PH[E] / ZE, J = F + Y / 2;
        RM(F, C - D / 2, Y, D, 5 * TM);
        QL("hsl(270 30% 8% / .8)");
        ZF.save();
        ZF.clip();
        ZF.fillStyle = XF(E, H ? 60 : 42, OK === E && PK > 0 ? .4 + NE(EH * 40) * .3 : 1);
        ZF.fillRect(F, C + D / 2 - D * I, Y, D * I);
        ZF.restore();
        RM(F, C - D / 2, Y, D, 5 * TM);
        PL((H ? 2.6 : 1.2) * TM, H ? NM : XF(E, 55, .8));
        FD("ROYGBIV"[E], J, C, H ? 15 : 12, H ? NM : OM, 1);
        FD(E + 1, J, C - D / 2 - 9 * TM, 10, H ? NM : PM);
        SH & DF[E] && RL(J, C + D / 2 + 8 * TM, 2.6 * TM, XF(E, 75));
        UM.push({
            x: J,
            y: C,
            w: Y,
            h: D,
            fn: () => UD(E)
        });
    }
    VH > 2 && FD(VH + "/7", B - 18 * TM, C, 13, NM, 1);
}

function LD() {
    if (DH !== 1 || QI || !LH.al) {
        return;
    }
    const Y = OI - LH.x, B = PI - LH.y, C = QE(Y, B);
    C > AE && RL(BM(LH.x + Y / C * AE), CM(LH.y + B / C * AE), 4 * TM, XF(QH, 60, .8));
}

function MD(Y, B, C, D, E, F) {
    ZF.fillStyle = "hsl(275 45% 5% / .8)";
    ZF.fillRect(0, 0, YH, BH);
    if (Y) {
        RM(YH / 2 - Y * TM / 2, BH / 2 - B * TM / 2, Y * TM, B * TM, 14 * TM);
        QL("hsl(272 40% 9% / .96)");
        PL(2 * TM, SM);
        FD(C, YH / 2, BH / 2 - (B / 2 - 34) * TM, 27, NM, 1);
    }
    for (let Y = 0; Y < D.length; Y++) {
        FD(D[Y], YH / 2, BH / 2 - (B / 2 - 78) * TM + Y * 22 * TM, F || 13.5, OM, F ? 1 : 0);
    }
    for (const Y of E) {
        HD(YH / 2 + Y[0] * TM, BH / 2 + Y[1] * TM, Y[2] * TM, 40 * TM, Y[3], Y[4], Y[5]);
    }
}

function ND() {
    LI.m ^= 1;
    TK && (TK.gain.value = LI.m ? 0 : .8);
    XD();
}

function OD() {
    MD(0, 0, 0, [], [ [ 0, -30, 190, "PLAY", AD, "hsl(300 80% 55%)" ], [ -105, 26, 130, "STORE", () => {
        DH = 4;
    } ], [ 105, 26, 130, LI.m ? "UNMUTE" : "MUTE", ND ] ]);
    const Y = BH * .3;
    for (let B = 0; B < 7; B++) {
        ZF.font = "bold " + (66 * TM | 0) + "px monospace";
        ZF.textAlign = "center";
        ZF.fillStyle = XF(B, 60, .5);
        ZF.fillText("PRISMFALL", YH / 2 + NE(EH * 1.2 + B * .5) * 5 * TM, Y + 2.4 * (B - 3) * TM);
    }
    FD("PRISMFALL", YH / 2, Y, 66, NM, 1);
    FD("you never steer the unicorn - you draw the physics", YH / 2, Y + 46 * TM, 15, OM);
    [ "DRAG near the unicorn - drawings STAY until used - longer = stronger", "R push - O aim - Y spring - G tether - B rail - I gravity - V warp", 0, "X lets go - 1-7 or SCROLL picks a colour" ].forEach((Y, B) => Y && FD(Y, YH / 2, BH - 122 * TM + B * 19 * TM, 13, B === 1 ? OM : B > 2 ? PM : NM, !B));
    const B = ZF.createLinearGradient(YH / 2 - 300 * TM, 0, YH / 2 + 300 * TM, 0);
    for (let Y = 0; Y < 7; Y++) {
        B.addColorStop(Y / 6, XF(Y, 68));
    }
    FD("CROSS TWO STROKES TO MIX THEM - O+Y aims AND springs", YH / 2, BH - 84 * TM, 13, B, 1);
    LI.b && FD("BEST " + LI.b + "   DEPTH " + (LI.d / 10 | 0) + "m   COINS " + LI.c, YH / 2, BH / 2 + 82 * TM, 14, PM);
}

function PD() {
    MD(470, 330, "RUN OVER", [ "SCORE   " + (RH | 0), "DEPTH   " + (XH / 10 | 0) + "m", "REGION  " + TI[AH][0], "COINS   +" + UH, "BEST    " + LI.b, RH >= LI.b && RH ? "NEW BEST!" : "" ], [ [ 0, 62, 210, "RETRY  (R)", AD, "hsl(300 80% 55%)" ], [ -105, 114, 130, "STORE", () => {
        DH = 4;
    } ], [ 105, 114, 130, "MENU", () => {
        DH = 0;
    } ] ], 16);
}

function QD() {
    ZF.fillStyle = "hsl(275 45% 5% / .86)";
    ZF.fillRect(0, 0, YH, BH);
    const Y = BH / 2;
    for (let B = 0; B < 7; B++) {
        ZF.font = "bold " + (44 * TM | 0) + "px monospace";
        ZF.textAlign = "center";
        ZF.fillStyle = XF(B, 60, .45);
        ZF.fillText("PRISM ASCENSION", YH / 2 + NE(EH * 1.6 + B * .5) * 4 * TM, Y - 168 * TM + 1.8 * (B - 3) * TM);
    }
    FD("PRISM ASCENSION", YH / 2, Y - 168 * TM, 44, NM, 1);
    FD("DESCENT " + (descent + 1) + " DONE - the shaft begins again, harder", YH / 2, Y - 128 * TM, 15, OM);
    FD("TAKE ONE. IT IS YOURS FOR THE REST OF THE RUN.", YH / 2, Y - 104 * TM, 13, PM);
    dsel = -1;
    const B = 300 * TM, C = 190 * TM;
    for (let D = 0; D < draft.length; D++) {
        const E = draft[D], F = YH / 2 + (D - (draft.length - 1) / 2) * (B + 26 * TM), H = Y + 4 * TM, I = WM(F, H, B, C);
        I && (dsel = D);
        RM(F - B / 2, H - C / 2, B, C, 14 * TM);
        QL(I ? "hsl(285 45% 17% / .98)" : "hsl(272 40% 10% / .96)");
        PL((I ? 3 : 1.6) * TM, I ? NM : XF(E % 7, 60));
        RM(F - B / 2 + 16 * TM, H - C / 2 + 16 * TM, B - 32 * TM, 5 * TM, 3 * TM);
        QL(XF(E % 7, 62));
        FD(JF[E * 2], F, H - 34 * TM, 21, NM, 1);
        const J = JF[E * 2 + 1].split(" ");
        let K = "", L = 0;
        ZF.font = (13 * TM | 0) + "px monospace";
        for (let Y = 0; Y <= J.length; Y++) {
            const C = K ? K + " " + J[Y] : J[Y];
            if (Y < J.length && ZF.measureText(C).width < B - 44 * TM) {
                K = C;
            } else {
                FD(K, F, H + (4 + 19 * L++) * TM, 13, OM);
                K = J[Y] || "";
            }
        }
        FD("PRESS " + (D + 1), F, H + C / 2 - 22 * TM, 12, I ? NM : PM, 1);
        UM.push({
            x: F,
            y: H,
            w: B,
            h: C,
            fn: () => BE(D)
        });
    }
}

function SD() {
    MD(340, 296, "PAUSED", [], [ [ 0, -40, 200, "RESUME", () => {
        DH = 1;
    } ], [ 0, 8, 200, "RESTART  (R)", AD ], [ 0, 56, 200, LI.m ? "UNMUTE" : "MUTE", ND ], [ 0, 104, 200, "QUIT RUN", () => {
        ZD();
        DH = 3;
    } ] ]);
}

function VD() {
    MD(560, 470, "PRISM STORE", [ LI.c + " COINS - cosmetics only, never power" ], [ [ 0, 200, 150, "BACK", XM ] ], 14);
    AM = -1;
    const Y = YH / 2 - 240 * TM;
    for (let B = 0; B < 12; B++) {
        const C = B / 3 | 0, D = B % 3, E = Y + D * 150 * TM, F = BH / 2 - 118 * TM + C * 48 * TM, H = MM(C, D), I = LI.e[C] === D, J = WM(E + 66 * TM, F, 140 * TM, 38 * TM);
        if (J) {
            AM = C;
            ZM = D;
        }
        RM(E, F - 19 * TM, 140 * TM, 38 * TM, 7 * TM);
        QL(I ? SM : H ? QM : "hsl(275 25% 10%)");
        PL((I ? 2.4 : 1) * TM, I || J ? NM : PM);
        FD(KM[B], E + 66 * TM, F - 5 * TM, 11, H ? NM : OM, 1);
        FD(H ? I ? "EQUIPPED" : "EQUIP" : LM[D] + "c", E + 66 * TM, F + 9 * TM, 10, H ? PM : LI.c < LM[D] ? "hsl(0 60% 60%)" : VM);
        UM.push({
            x: E + 66 * TM,
            y: F,
            w: 140 * TM,
            h: 38 * TM,
            fn: () => TD(C, D)
        });
    }
    ZF.save();
    ZF.translate(YH / 2, BH / 2 + 122 * TM);
    ZF.scale(1.9 * TM, 1.9 * TM);
    DD(AM === 0 ? ZM : LI.e[0], SE(EH * 3) % 7, 0, AM === 1 ? ZM : LI.e[1]);
    ZF.restore();
}

function TD(Y, B) {
    if (MM(Y, B)) {
        LI.e[Y] = B;
        XD();
    } else if (LI.c >= LM[B]) {
        LI.c -= LM[B];
        LI.o |= 1 << Y * 3 + B;
        LI.e[Y] = B;
        XD();
        YC();
    } else {
        BC();
    }
}

function UD(Y) {
    if ((Y = (Y + 7) % 7) !== QH) {
        QH = Y;
        EC(1);
        LC(LH.x, LH.y, 6, 0, 130, CF[Y]);
        if (SI === 1) {
            SI = 2;
            VI = 0;
        }
    }
}

function RD() {
    const Y = LE(devicePixelRatio || 1, 2);
    YH = AF.width = innerWidth * Y | 0;
    BH = AF.height = innerHeight * Y | 0;
    TM = LE(YH / 1280, BH / 720);
}

function WD(Y) {
    const B = YH / innerWidth;
    MI = Y.clientX * B;
    NI = Y.clientY * B;
    OI = DM(MI);
    PI = EM(NI);
}

function XD() {
    try {
        localStorage[MF] = [ LI.c, LI.b, LI.d, LI.o, LI.m, LI.t ].concat(LI.e).join();
    } catch (Y) {}
}

function AD(Y) {
    DB();
    DH = 1;
    RH = 0;
    UH = 0;
    WH = 1;
    XH = 0;
    AH = 0;
    ZH = 3.2;
    SH = 0;
    VH = 0;
    NK = 0;
    TH = 0;
    BI = 0;
    CI = 0;
    CN = 0;
    FH = 0;
    HJ = null;
    HH = 0;
    IH = 0;
    KH = 0;
    boon = 0;
    draft = [];
    dsel = -1;
    descent = 0;
    PH = [ ZE, ZE, ZE, ZE, ZE, ZE, ZE ];
    YI = [ 0, 0, 0, 0, 0, 0, 0 ];
    EI = [];
    FI = [];
    HI = [];
    II = [];
    JI = [];
    KI = [];
    QI = null;
    LH.x = 0;
    LH.y = -320;
    LH.vy = 300;
    LH.a = HE / 2;
    LH.ra = null;
    LH.te = null;
    LH.ph = 0;
    LH.rp = 0;
    LH.st = 0;
    LH.gt = 0;
    LH.al = 1;
    LH.sp = 300;
    LH.rw = 0;
    MH = 0;
    NH = UE;
    OH.x = LH.x;
    OH.y = LH.y;
    OH.z = 1;
    AL = HM(0);
    BY(Y);
    LH.vx = SF(-70, 70);
    SI = LI.t ? 3 : 0;
    VI = 0;
    EL = 0;
    BL && (DL = BL.currentTime + .05);
    PB(0);
}

function ZD() {
    LI.c += UH;
    LI.b = ME(LI.b, RH | 0);
    LI.d = ME(LI.d, XH | 0);
    LI.t || (LI.t = 1);
    XD();
    PB(0);
}

function YE() {
    const Y = [];
    for (let B = 0; B < KF; B++) {
        LF(B) || Y.push(B);
    }
    if (Y.length) {
        draft = [ Y.splice(SE(B() * Y.length), 1)[0] ];
        Y.length && draft.push(Y[SE(B() * Y.length)]);
        dsel = -1;
        DH = 5;
        IH = 1;
        JH = -1;
        XB();
    } else {
        descent++;
    }
}

function BE(Y) {
    if (!(DH !== 5 || Y >= draft.length)) {
        boon |= 1 << draft[Y];
        descent++;
        DH = 1;
        for (let Y = 0; Y < 7; Y++) {
            PH[Y] = SK();
        }
        RH += 5e3 * WH | 0;
        OC(LH.x, LH.y - 40, JF[draft[Y] * 2], -1);
        draft = [];
        WB();
    }
}

function CE() {
    LH.al = 0;
    CN = 1.3;
    for (let Y = 0; Y < 7; Y++) {
        LC(LH.x, LH.y, 16, 0, 520, CF[Y]);
    }
    KC(LH.x, LH.y, 0, 0, .7, -1, 2, 22);
    HH = 30;
    IH = .7;
    JH = -1;
    DC();
    LH.ra && IY(0);
    LH.te && KY();
    DH !== 0 && ZD();
}

function DE(Y) {
    CY();
    HJ = null;
    for (const Y of DI) {
        Y.v && LH.y > Y.y + 120 && LH.y < Y.y + Y.h - 120 && (HJ = Y);
    }
    const B = FH;
    FH = PF(FH, HJ ? 1 : 0, 3.5, Y);
    if (B > .5 && FH <= .5) {
        LC(LH.x, LH.y, 14, 0, 420, CF[6]);
        MB(1);
    }
    LH.al && EE(Y);
    SY(Y);
    if (DH === 1 && DK(LH.y) > descent && LH.al) {
        YE();
        return;
    }
    const C = CK(LH.y);
    if (C !== AH) {
        AH = C;
        ZH = 3.2;
        IH = ME(IH, .35);
        JH = WI(AH);
        FC();
        RH += 500 * WH | 0;
    }
    if (LH.y > XH) {
        RH += .05 * (LH.y - XH) * WH;
        XH = LH.y;
    }
    WH = NF(1 + LH.sp / 950 + VH * .2 + (TH > 0 ? 2 : 0), 1, 12);
    if (LH.al) {
        const B = 2.4 * (LF(5) ? 1.8 : 1);
        if (LH.sp < 118) {
            LH.st += Y;
            const C = NF((LH.st - BF) / (B - BF), 0, 1);
            if ((DN -= Y) <= 0 && C > 0) {
                DN = ME(.16, .55 - C * .4);
                CC(C);
            }
            LH.st > B && CE();
        } else if (LH.st > 0) {
            LH.st > BF && LC(LH.x, LH.y, 8, 0, 260, CF[0]);
            LH.st = ME(0, LH.st - Y * 2.6);
        }
    } else {
        CN -= Y;
        if (CN <= 0) {
            if (DH === 0) {
                AD();
                DH = 0;
            } else {
                DH = 3;
            }
        }
    }
    ZH -= Y;
    PK -= Y;
    TH -= Y;
    for (let B = 0; B < 7; B++) {
        YI[B] -= Y;
    }
    CI > 0 && (CI -= Y) <= 0 && (BI = 0);
    if (NK > 0 && (NK -= Y) <= 0) {
        SH = 0;
        VH = 0;
    }
    IH = ME(0, IH - Y * 2.4);
    for (let B = EI.length; B--; ) {
        const C = EI[B];
        C.u && (C.l -= Y);
        if (C.u && C.l <= 0 || C.y2 < LH.y - 2600) {
            LH.ra === C && IY(1);
            C === QI && (QI = null);
            EI.splice(B, 1);
        }
    }
    PC(Y);
    LH.al && BD();
    if (!LI.t) {
        VI += Y;
        if (SI === 2 && VI > 9) {
            SI = 3;
            LI.t = 1;
            XD();
        }
    }
}

function EE(Y) {
    YN += Y;
    let B = 0;
    while (YN >= EN && B < 8) {
        TY(EN);
        YN -= EN;
        B++;
    }
    B >= 8 && (YN = 0);
}

function FE() {
    UM = [];
    TM = LE(YH / 1280, BH / 720);
    VC();
    RC();
    ZC();
    CD();
    YD();
    LH.al && ED();
    DH !== 1 && DH !== 2 || JD();
    if (IH > .01) {
        ZF.fillStyle = JH < 0 ? "hsl(" + (EH * 500 % 360 | 0) + " 100% 75% / " + IH * .5 + ")" : WF(JH | 0, 100, 72, IH * .45);
        ZF.fillRect(0, 0, YH, BH);
    }
    DH > 1 ? [ 0, 0, SD, PD, VD, QD ][DH]() : DH || OD();
    LD();
}

const HE = Math.PI, IE = HE * 2, JE = Math, KE = JE.abs, LE = JE.min, ME = JE.max, NE = JE.sin, OE = JE.cos, PE = JE.atan2, QE = JE.hypot, SE = JE.floor, VE = JE.random, TE = 17, UE = 1250, RE = 2900, WE = 400, XE = 545, AE = 305, ZE = 100, YF = [ .088, .055, .062, .07, .05, .076, .095 ], BF = .9, CF = [ 0, 32, 54, 140, 200, 248, 312 ], DF = [ 1, 2, 4, 8, 16, 32, 64 ], EF = 64, FF = 15e3, HF = [ [ 0, 1.5, 10 ], [ 2, 1.5, 11 ], [ 3, 1.7, 12 ], [ 4, 2, 12 ], [ 5, 1.9, 11 ], [ 6, 1.9, 11 ], [ 7, .4, 12 ] ], IF = "OVERDRIVE SUPERCOIL REACH SUPERRAIL FLUX ECHO EFFICIENCY".split(" "), JF = "PRISM HEART|+30% pigment, refilled|AFTERGLOW|strokes cost 30% less|MOMENTUM|far less drag at speed|DEMOLITION|panels break at half the hit|LODESTONE|coins always pull to you|SECOND WIND|stall clock +80%|RESONANCE|bumpers pay double|WIDE PALETTE|+2 stroke slots|HARD LIGHT|strokes fire twice|GOLDEN HOUR|coins worth triple".split("|"), KF = JF.length >> 1, LF = Y => boon >> Y & 1, MF = "pf26_save", NF = (Y, B, C) => Y < B ? B : Y > C ? C : Y, OF = (Y, B, C) => Y + (B - Y) * C, PF = (Y, B, C, D) => OF(Y, B, 1 - JE.exp(-C * D));

let QF = 1;

const SF = (Y, C) => Y + B() * (C - Y), VF = (Y, C) => SE(Y + B() * (C - Y + 1)), TF = Y => B() < Y, UF = Y => Y[SE(B() * Y.length)], RF = () => B() < .5 ? -1 : 1, WF = (Y, B, C, D) => "hsl(" + Y + " " + B + "% " + C + "%" + (D === void 0 ? "" : " / " + D) + ")", XF = (Y, B, C) => WF(CF[Y], 100, B, C), AF = document.getElementById("a"), ZF = AF.getContext("2d");

let YH = 0, BH = 0, CH = 1, DH = 0, EH = 0, FH = 0, HH = 0, IH = 0, JH = 0, KH = 0;

const LH = {
    x: 0,
    y: 0,
    vx: 0,
    vy: 0,
    a: 0,
    sp: 0,
    ra: null,
    rt: 0,
    rs: 1,
    rw: 0,
    te: null,
    ph: 0,
    rp: 0,
    st: 0,
    gt: 0,
    al: 1
};

let MH = 0, NH = UE;

const OH = {
    x: 0,
    y: 0,
    z: 1,
    tz: 1
};

let PH = [], QH = 1, SH = 0, VH = 0, TH = 0, UH = 0, RH = 0, WH = 1, XH = 0, AH = 0, ZH = 0, YI = [], BI = 0, CI = 0, DI = [], EI = [], FI = [], HI = [], II = [], JI = [], KI = [];

const LI = {
    c: 0,
    b: 0,
    d: 0,
    o: 0,
    e: [ 0, 0, 0, 0 ],
    m: 0,
    t: 0
};

let MI = 0, NI = 0, OI = 0, PI = 0, QI = null, SI = 0, VI = 0;

const TI = [ [ "CLOUDBREAK", 206, 74, 88, 26, "0002100210", 5, 1, 5, 40 ], [ "SUNFORGE", 12, 34, 72, 19, "8574185741", 7, 3, 4, 196 ], [ "VERDANT COIL", 152, -74, 74, 17, "2622762262", 8, 5, 6, 320 ], [ "CRYSTAL CURRENT", 190, 44, 84, 15, "3303531333", 5, 6, 7, 25 ], [ "PRISM MINE", 276, -28, 62, 10, "6760671666", 9, 2, 6, 120 ], [ "INVERSION TEMPLE", 264, 52, 80, 13, "6465264636", 7, 4, 7, 75 ], [ "RAINBOW ENGINE", 300, -136, 90, 9, "0123456788", 9, 0, 9, -1 ] ], UI = [ 180, 786794, 131494, 69971, 693042, 303957, 414347 ], RI = "120132410656", WI = Y => TI[Y][1], XI = Y => UI[Y], AI = (Y, B) => (Y >> 16 - B * 4 & 15) / 16, ZI = Y => Y > 5 ? [ VF(0, 6), VF(0, 6) ] : [ +RI[Y * 2], +RI[Y * 2 + 1] ], YJ = Y => TI[Y][7];

let BJ = 0, CJ = -400, DJ = WE, EJ = 0, FJ = 1, HJ = null, IJ = 0, JJ = 0, KJ = 0, LJ = 0, MJ = 0, NJ = 0;

const OJ = (Y, B, C, D, E) => Object.assign({
    t: 0,
    x: Y,
    y: B,
    r: C,
    g: 0,
    m: D | 0
}, E), PJ = (Y, B, C, D, E, F) => Object.assign({
    t: 1,
    x: Y,
    y: B,
    r: C,
    g: D,
    m: E | 0
}, F), QJ = (Y, B, C, D, E, F) => PJ((Y + C) / 2, (B + D) / 2, QE(C - Y, D - B) / 2, PE(D - B, C - Y), E, F), SJ = (Y, B, C, D) => ({
    t: Y,
    x: B,
    y: C,
    c: D | 0,
    g: 0
});

let VJ = 0, TJ = 0, UJ = 0, RJ = 0, WJ = 0, XJ = 0, AJ = 0, ZJ = 0, YK = 0, BK = 0;

const CK = Y => {
    const B = SE(ME(Y, 0) / FF);
    return B < 7 ? B : B % 7;
}, DK = Y => SE(SE(ME(Y, 0) / FF) / 7), EK = Y => NF(SE(ME(Y, 0) / FF) * .085 + DK(Y) * .3, 0, 2.4);

let FK = -545, HK = XE, IK = 0, JK = 0, KK = [], LK = 0;

const MK = Y => Y.m & 2 ? 280 : Y.m & 4 ? 20 : Y.m & 8 ? 285 : AL[6];

let NK = 0, OK = -1, PK = 0;

const QK = () => UY(7) * (LF(1) ? .7 : 1), SK = () => LF(0) ? 130 : ZE, VK = Y => .45 + LE(Y, 210) / 105 * .55;

let TK, UK, RK, WK, XK, AK, ZK, YL, BL = null, CL = 0, DL = 0, EL = 0;

const FL = Y => 440 * JE.pow(2, (Y - 69) / 12), HL = [ [ 0, 2, 3, 5, 7, 8, 10 ], [ 0, 2, 4, 5, 7, 9, 11 ], [ 0, 3, 5, 7, 10, 12 ], [ 0, 1, 5, 6, 8, 11 ] ], IL = [ "triangle", "sawtooth", "square" ], JL = () => BL && !LI.m && CL < 26, KL = () => BL.currentTime, LL = [ 0, 4, 7, 12 ], ML = [ 4369, 4437, 4115, 4369, 4097, 1298, 21845 ], NL = [ 34953, 42149, 37266, 52429, 33924, 43177, 43691 ], OL = () => ZF.beginPath(), PL = (Y, B) => {
    ZF.lineWidth = Y;
    ZF.strokeStyle = B;
    ZF.stroke();
}, QL = Y => {
    ZF.fillStyle = Y;
    ZF.fill();
}, SL = (Y, B, C, D, E) => ZF.arc(Y, B, C, D || 0, E === void 0 ? IE : E), VL = (Y, B) => ZF.moveTo(Y, B), TL = (Y, B) => ZF.lineTo(Y, B), UL = (Y, B, C, D, E, F) => {
    OL();
    VL(Y, B);
    TL(C, D);
    PL(E, F);
}, RL = (Y, B, C, D, E, F) => {
    OL();
    SL(Y, B, C);
    D && QL(D);
    E && PL(F, E);
}, WL = (Y, B, C, D, E) => {
    OL();
    for (let B = 0; B < Y; B++) {
        E(B);
    }
    ZF.closePath();
    B && QL(B);
    C && PL(D, C);
}, XL = (Y, B, C) => (Y ? TL : VL)(B, C);

let AL = [ 232, 62, 12, 322, 52, 27, 288, 80 ], ZL = 0, YM = 0;

const BM = Y => YH / 2 + (Y - OH.x) * CH + ZL, CM = Y => BH / 2 + (Y - OH.y) * CH + YM, DM = Y => OH.x + (Y - YH / 2 - ZL) / CH, EM = Y => OH.y + (Y - BH / 2 - YM) / CH, FM = (Y, B, C, D) => (Y + ((B - Y + 540) % 360 - 180) * (1 - JE.exp(-C * D)) + 360) % 360, HM = Y => {
    const B = TI[Y], C = B[1], D = B[4], E = B[9];
    return [ C, 56, D, C + B[2], 64, D + 23, E < 0 ? EH * 16 % 360 : E, B[3] ];
}, IM = [ [ 0, 3, 0 ], [ 2, 1, 8 ], [ 2, 2, -3 ], [ 1, 5, 4 ], [ 1, 7, -1 ], [ 2, 3, 6 ], [ 2, 1, 4 ] ], JM = [ [ 276, 40, 7, 60, 26 ], [ 18, 74, 42, 100, 66 ], [ 292, 60, 30, 100, 74 ], [ 200, 80, 40, 100, 76 ], [ 140, 70, 34, 100, 70 ], [ 46, 38, 22, 90, 60 ], [ 46, 100, 60, 100, 92 ] ], KM = "CLOUD SHADOW NEON SPIRAL LANCE STARTIP RAINBOW DASHED COMET SPARKS SHARDS RINGS".split(" "), LM = [ 0, 180, 420 ], MM = (Y, B) => !B || LI.o >> Y * 3 + B & 1, NM = "#fff", OM = "hsl(0 0% 100% / .62)", PM = "hsl(0 0% 100% / .34)", QM = "hsl(282 40% 15% / .88)", SM = "hsl(290 55% 50%)", VM = "hsl(48 100% 66%)";

let TM = 1, UM = [];

const RM = (Y, B, C, D, E) => {
    OL();
    ZF.roundRect(Y, B, C, D, E);
}, WM = (Y, B, C, D) => MI > Y - C / 2 && MI < Y + C / 2 && NI > B - D / 2 && NI < B + D / 2, XM = () => {
    DH = LI.b || RH ? 3 : 0;
};

let AM = -1, ZM = -1;

document.body.style.cssText = "margin:0;overflow:hidden;background:#05030c";

AF.style.cssText = "position:fixed;width:100%;height:100%";

addEventListener("resize", RD);

addEventListener("contextmenu", Y => Y.preventDefault());

addEventListener("pointermove", Y => {
    WD(Y);
    QI && WY();
});

addEventListener("pointerdown", Y => {
    DB();
    WD(Y);
    Y.button || ID() || DH !== 1 || RY();
});

addEventListener("pointerup", () => {
    QI = null;
});

addEventListener("blur", () => {
    QI = null;
    DH === 1 && (DH = 2);
});

addEventListener("wheel", Y => {
    if (DH === 1) {
        Y.preventDefault();
        UD(QH + (Y.deltaY > 0 ? 1 : -1));
    }
}, {
    passive: 0
});

addEventListener("keydown", Y => {
    DB();
    const B = Y.key, C = B.toLowerCase();
    if (DH !== 5) {
        if (B > "0" && B < "8") {
            UD(+B - 1);
        } else {
            B !== "Escape" && C !== "p" || (DH = DH === 1 ? 2 : DH === 2 ? 1 : DH === 4 ? (XM(), 
            DH) : DH);
            C === "r" && DH > 1 && DH !== 5 && AD();
            C === "m" && ND();
            C === "x" && (LH.ra ? IY(1) : LH.te && KY());
            B !== " " && B !== "Enter" || (DH === 2 ? DH = 1 : DH < 4 && AD());
        }
    } else {
        B !== "1" && B !== "2" || BE(+B - 1);
    }
});

document.addEventListener("visibilitychange", () => {
    document.hidden && DH === 1 && (DH = 2);
});

let YN = 0, BN = 0, CN = 0, DN = 0;

const EN = 1 / 120;

(() => {
    try {
        const Y = (localStorage[MF] || "").split(",").map(Number);
        if (Y.length < 9 || Y.some(isNaN)) {
            return;
        }
        LI.c = NF(Y[0] | 0, 0, 1e9);
        LI.b = NF(Y[1] | 0, 0, 1e12);
        LI.d = NF(Y[2] | 0, 0, 1e12);
        LI.o = Y[3] | 0;
        LI.m = Y[4] ? 1 : 0;
        LI.t = Y[5] ? 1 : 0;
        for (let B = 0; B < 4; B++) {
            const C = Y[6 + B] | 0;
            LI.e[B] = C > 0 && C < 3 && MM(B, C) ? C : 0;
        }
    } catch (Y) {}
})();

RD();

AD();

DH = 0;

LH.al = 1;

requestAnimationFrame(function Y(B) {
    requestAnimationFrame(Y);
    const C = NF((B - BN) / 1e3, 0, .05);
    BN = B;
    let D = C * OF(1, .4, FH);
    if (KH > 0) {
        KH -= C;
        D *= .12;
    }
    EH += D;
    DH === 1 || DH === 0 ? DE(D) : DH === 3 && PC(D);
    QC(C);
    SC(C);
    HC();
    FE();
});