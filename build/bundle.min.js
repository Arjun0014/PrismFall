function K(z) {
    for (let y = 0; y < 7; y += 1) {
        if (XU[y] > 0 && YV[y][0] == z) {
            return YV[y][1];
        }
    }
    return 1;
}

function L() {
    if (H != 1 || !_.j) {
        return;
    }
    if (MU[NU] <= .5) {
        OT = NU;
        PT = .5;
        ZY();
        return;
    }
    let z = KT - _.x;
let y = LT - _.y;
let x = I(1, QV / (IV(z, y) || 1));
    let w = _.x + z * x;
let v = _.y + y * x;
    MT = {
        p: w,
        q: v,
        v: w,
        A: v,
        e: VV[NU],
        c: NU,
        l: 0,
        u: 0,
        B: 0
    };
    BT.push(MT);
    while (BT.length > 5) {
        if (_.C == BT.shift()) {
U(0);
}
    }
    if (_.D) {
W();
}
}

function M() {
    let z = MT;
    if (!z) {
        return;
    }
    let y = KT - z.p;
let x = LT - z.q;
let w = IV(y, x);
    if (w < 1) {
        return;
    }
    let v = I(w, 210);
    if (v > z.B) {
        let y = SV[z.c] * K(7);
let x = (v - z.B) * y;
        if (MU[z.c] >= x) {
            MU[z.c] -= x;
            z.B = v;
        } else {
            v = z.B += MU[z.c] / y;
            MU[z.c] = 0;
            if (PT <= 0) {
                OT = z.c;
                PT = .6;
                ZY();
            }
        }
    }
    z.v = z.p + y / w * v;
    z.A = z.q + x / w * v;
    N(z);
}

function N(z) {
    for (let y of BT) {
        if (y == z || y.u || y.e == z.e) {
            continue;
        }
        let x = JX(z.p, z.q, z.v, z.A, y.p, y.q, y.v, y.A);
        if (x) {
            z.e = y.e = z.e | y.e;
            FT.push({
                x: x[0],
                y: x[1],
                t: .55
            });
            YY();
        }
    }
}

function O(z) {
    let y = z.v - z.p;
let x = z.A - z.q;
let w = IV(y, x);
    if (z.u || z == MT || z == _.C || w < 22) {
        return 0;
    }
    let v = IX(z.p, z.q, z.v, z.A, _.x, _.y);
    let u = z.p + y * v;
let t = z.q + x * v;
    let s = _.x - u;
let r = _.y - t;
    let q = IV(s, r);
    if (q > 22) {
        return 0;
    }
    let p = y / w;
let o = x / w;
    if (q < .001) {
        s = -o;
        r = p;
    } else {
        s /= q;
        r /= q;
    }
    P(z, s, r, u, t, v, p, o, w);
    ST = .05;
    return 1;
}

function P(z, y, x, w, v, u, t, s, r) {
    let q = z.e;
    let p = QT(r);
    let o = 1;
    if (q & 64) {
        let w = K(6);
        if (q & 16) {
            _.E = w * .8;
        } else {
            let v = 0;
            for (let y of BT) {
                if (y != z && !y.u && y.e & 64) {
                    v = y;
                    break;
                }
            }
            NX(_.x, _.y);
            if (v) {
                let z = HV(v.A - v.q, v.v - v.p) - HV(s, t);
let y = GV(z);
let x = FV(z);
                let w = _.F * y - _.G * x;
let u = _.F * x + _.G * y;
                let r = 30 / (IV(w, u) || 1);
                _.F = w;
                _.G = u;
                _.x = (v.p + v.v) / 2 + w * r;
                _.y = (v.q + v.A) / 2 + u * r;
                v.u = 1;
                v.l = .12;
            } else {
                let z = IV(_.F, _.G);
                let v = w * 300 * p;
                if (z < 60) {
                    _.x += y * v;
                    _.y += x * v;
                    _.F = y * 460;
                    _.G = x * 460;
                } else {
                    let y = v / z;
                    _.x += _.F * y;
                    _.y += _.G * y;
                }
            }
            CY(_.y);
            _.x = J(_.x, CR + LV, DR - LV);
            _.E = w * .34 * p;
            _.F *= 1.07;
            _.G *= 1.07;
            NX(_.x, _.y);
        }
        TY();
    }
    if (q & 16) {
        if (_.D) {
W();
}
        _.C = z;
        _.H = u;
        _.I = K(4) > 1 ? 4 : 0;
        _.J = y * -s + x * t >= 0 ? 1 : -1;
        let w = F(IV(_.F, _.G), 340 + p * 180);
        let v = _.F * t + _.G * s >= 0 ? 1 : -1;
        _.F = t * w * v;
        _.G = s * w * v;
        o = 0;
        RY(1);
    } else if (q & 8) {
        if (_.C) {
U(0);
}
        let y = K(3);
        _.D = {
            x: z.p,
            y: z.q,
            l: F(r * y, 30),
            t: (1.4 + p) * y
        };
        QY(1);
    }
    if (q & 32) {
        JU = y * MV;
        KU = x * MV;
        _.K = 2.9 * K(5) * p;
        SY(x);
        MX(w, v, 10, 3, 150, UV[5]);
    }
    let n = q & 4;
    if (q & 2) {
        let z = IV(_.F, _.G);
        let y = n ? F(z * 1.34, 560 + p * 320) : F(z * .99, 250 + p * 250);
        _.F = t * y;
        _.G = s * y;
        OY(n);
    } else if (n) {
        let z = _.F * y + _.G * x;
let w = (1.25 + p * .6) * K(2);
        _.F -= (1 + w) * z * y;
        _.G -= (1 + w) * z * x;
        let v = _.F * y + _.G * x;
let u = I(300 + p * 300, EV(z) * 1.6);
        if (v < u) {
            _.F += (u - v) * y;
            _.G += (u - v) * x;
        }
        PY(EV(z));
    } else if (!(q & 89)) {
        let z = _.F * y + _.G * x;
        if (z < 0) {
            _.F -= z * 1.55 * y;
            _.G -= z * 1.55 * x;
        }
    }
    if (q & 1) {
        let z = K(0);
        let u;
let t;
let s = IV(_.F, _.G);
        if (s < 50) {
            u = y;
            t = x;
            s = 0;
        } else {
            u = _.F / s;
            t = _.G / s;
            let z = u * y + t * x;
            if (z < 0) {
                u -= z * 2 * y;
                t -= z * 2 * x;
            }
        }
        let r = s * 1.5 + z * 440 * p;
        _.F = u * r;
        _.G = t * r;
        _.M = .85;
        FU = I(26, FU + 7);
        KX(w, v, 120 + p * 160, UV[0]);
        MX(w, v, 26, 0, 220, UV[0], 420 * -u, 420 * -t);
        let q = (90 + p * 110) * z;
        for (let z of RT) {
            for (let y of z.o) {
                if (!y.k && !y.N && y.m & 4 && IV(y.x - w, y.y - v) < q + y.r) {
                    y.N = .02;
                    y.O = 0;
                }
            }
        }
        NY(r);
    }
    T();
    if (o) {
        z.u = 1;
        z.l = .22;
    }
    OX(z, w, v);
    Q(q);
}

function Q(z) {
    let y = OU;
    NT = 7;
    OU |= z & 127;
    if (OU == y) {
        return;
    }
    let x = 0;
    for (let z = 0; z < 7; z += 1) {
        if (OU & VV[z]) {
x += 1;
}
    }
    if (!(x <= PU)) {
        PU = x;
        if (x == 7) {
            return R();
        }
        if (x == 3 || x == 5) {
            for (let z = 0; z < 7; z += 1) {
                MU[z] = I(RV, MU[z] + 6);
            }
            PX(_.x, _.y - 30, "SPECTRUM " + x + "/7", UV[x]);
            UY(0);
        }
    }
}

function R() {
    QU = 3.2;
    for (let z = 0; z < 7; z += 1) {
        MU[z] = I(RV, MU[z] + 26);
    }
    TU = I(12, TU + 1.5);
    SU += TU * 1200 | 0;
    GU = 1;
    HU = -1;
    FU = I(32, FU + 14);
    PX(_.x, _.y - 46, "FULL SPECTRUM", -1);
    XY();
    OU = 0;
    PU = 0;
}

function S(z) {
    z.g = 1;
    if (z.t == 0) {
        YU += 1;
        ZU = 1.4;
        RU += 1;
        SU += TU * 10 * (1 + YU * .05) | 0;
        UY(YU);
    } else if (z.t == 1) {
        RU += 15;
        SU += TU * 500 | 0;
        PX(z.x, z.y, "CROWN +15", 48);
        GU = F(GU, .45);
        HU = 48;
        WY();
    } else if (z.t == 2) {
        MU[z.c] = I(RV, MU[z.c] + 40);
        UY(z.c);
        MX(z.x, z.y, 12, 3, 190, UV[z.c]);
    } else if (z.t == 3) {
        for (let z = 0; z < 7; z += 1) {
            MU[z] = RV;
        }
        GU = 1;
        HU = -1;
        PX(z.x, z.y, "PRISM WELL", -1);
        WY();
    } else {
        XU[z.c] = YV[z.c][2];
        PX(z.x, z.y, ZV[z.c], YV[z.c][0] > 6 ? -1 : UV[YV[z.c][0]]);
        VY(60, [ 0, 5, 10 ], .12, .1, "square", .06, 2);
    }
}

function T() {
    let z = IV(_.F, _.G);
    if (z > NV) {
        _.F = _.F / z * NV;
        _.G = _.G / z * NV;
    }
    if (!isFinite(z)) {
        _.F = 0;
        _.G = 200;
    }
    if (!isFinite(_.x + _.y)) {
        _.x = 0;
        _.y = UU;
    }
}

function U(z) {
    let y = _.C;
    if (y) {
        y.u = 1;
        y.l = I(y.l, .12);
    }
    _.C = 0;
    if (z) {
        _.F *= 1.06;
        _.G *= 1.06;
    }
    RY(0);
}

function V(z) {
    let y = _.C;
    let x = y.v - y.p;
let w = y.A - y.q;
let v = IV(x, w);
    if (y.u || v < 8) {
        U(1);
        return 0;
    }
    let u = x / v;
let t = w / v;
    let s = _.F * u + _.G * t;
    s = s * .998 + (JU * u + KU * t) * z;
    if (EV(s) < 370) {
        let z = JU * u + KU * t;
        s = 370 * (z ? z > 0 ? 1 : -1 : s >= 0 ? 1 : -1);
    }
    _.H += s * z / v;
    _.F = u * s;
    _.G = t * s;
    if (_.H < 0 || _.H > 1) {
        _.H = J(_.H, 0, 1);
        if (!(_.I > 0)) {
            U(1);
            return 0;
        }
        _.I -= z;
        _.F = -_.F;
        _.G = -_.G;
        _.H = J(_.H, .02, .98);
        PY(400);
    }
    let r = u * _.J;
    _.x = y.p + x * _.H + -t * _.J * 22;
    _.y = y.q + w * _.H + r * 22;
    if (KV() < .5) {
MX(_.x, _.y, 3, 0, 60, UV[4], .1 * -_.F, .1 * -_.G);
}
    return 1;
}

function W() {
    if (!_.D) {
        return;
    }
    _.D = 0;
    let z = 1.34 + 150 / (IV(_.F, _.G) || 1);
    _.F *= z;
    _.G *= z;
    T();
    MX(_.x, _.y, 10, 0, 260);
    QY(0);
}

function X() {
    let z = _.D;
    let y = _.x - z.x;
let x = _.y - z.y;
    let w = IV(y, x);
    if (w < 1 || w <= z.l) {
        return;
    }
    y /= w;
    x /= w;
    _.x = z.x + y * z.l;
    _.y = z.y + x * z.l;
    let v = _.F * y + _.G * x;
    if (v > 0) {
        _.F -= v * y;
        _.G -= v * x;
    }
    let u = IV(_.F, _.G);
    if (u > 1 && u < 1500) {
        let z = F(1.004, 320 / u);
        _.F *= z;
        _.G *= z;
    }
}

function Y(z) {
    let y = TU > 4 ? 60 + TU * 24 : 0;
    for (let x of RT) {
        for (let w of x.i) {
            if (w.g) {
                continue;
            }
            let x = _.x - w.x;
let v = _.y - w.y;
let u = IV(x, v);
            if (u < 43) {
                S(w);
            } else if (y && !w.t && u < y) {
                let y = I(1, z * 700 / u);
                w.x += x * y;
                w.y += v * y;
            }
        }
    }
}

function Z(z) {
    for (let z of RT) {
        for (let y of z.o) {
            y.k || _Z(y);
        }
    }
    if (ST > 0) {
        ST -= z;
    } else {
        for (let z = 0; z < BT.length; z += 1) {
            if (O(BT[z])) {
                break;
            }
        }
    }
}

function _Z(z) {
    HZ(z);
    let y = LZ(z, _.x, _.y) + LV;
    if (YS > y) {
        return;
    }
    let x = WS;
let w = XS;
let v = US;
let u = VS;
let t = y - YS;
    if (z.m & 8 && _.E > 0) {
        return;
    }
    IZ(z, v, u);
    let s = _.F - RS;
let r = _.G - SS;
    let q = s * x + r * w;
    if (z.m & 4) {
        if (-q > (_.M > 0 ? 300 : 900)) {
            CZ(z, v, u, 0, 0);
            return;
        }
    }
    _.x += x * t;
    _.y += w * t;
    if (_.C) {
U(0);
}
    if (q >= 0) {
        return;
    }
    let p = z.m & 1;
let o = z.m & 2;
    z.f = 1;
    if (z.m & WV) {
BZ(z, v, u);
}
    let n = (1 + (p ? 1.32 - J(_.P / NV, 0, 1) * .72 : o ? .12 : .44)) * -q;
    s += n * x;
    r += n * w;
    let m = -w;
let l = x;
let k = s * m + r * l;
    let j = I(EV(k), (o ? 1.15 : .3) * n) * (k < 0 ? -1 : 1);
    s -= j * m;
    r -= j * l;
    _.F = s + RS;
    _.G = r + SS;
    T();
    let i = -q;
    KY(i, o ? 1 : p ? 2 : 0, z.x + z.y);
    FU = I(26, FU + i * (p ? .008 : .0035));
    MX(v, u, J(i * .012, 1, 14) | 0, 1, I(i * .35, 420), TT(z));
    if (p && i > 150) {
        YU += 1;
        ZU = 1.6;
        let z = (18 + i * .05) * TU | 0;
        SU += z;
        if (i > 620) {
            PX(v, u, "+" + z, UV[2]);
            KX(v, u, 60 + I(i * .2, 190), TQ[6] + 30);
            IU = F(IU, .045);
        }
    }
}

function BZ(z, y, x) {
    if (z.S) {
        return;
    }
    let w = z.T;
    z.S = 1;
    if (!w) {
        return;
    }
    w.l += 1;
    SU += TU * 60 | 0;
    UY(w.l / w.n * 8);
    MX(y, x, 10, 0, 260, 48);
    if (w.l < w.n) {
        return;
    }
    w.l = 0;
    for (let z of w.m) {
        z.S = 0;
    }
    let v = w.n * 420 * TU | 0;
    SU += v;
    RU += w.n * 2;
    for (let z = 0; z < 7; z += 1) {
        MU[z] = I(RV, MU[z] + 5);
    }
    PX(w.x, w.y, "BANK +" + v, 48);
    KX(w.x, w.y, 320, 48);
    GU = F(GU, .4);
    HU = 48;
    FU = I(30, FU + 12);
    IU = F(IU, .07);
    MY();
}

function CZ(z, y, x, w, v) {
    z.k = 1;
    let u = TT(z);
    FU = I(32, FU + 10 - v);
    MX(y, x, 18, 2, 460, u);
    EZ(y, x, z, u);
    KX(y, x, 150, u);
    LY(v);
    let t = TU * 60 * (1 + v * .5) | 0;
    SU += t;
    YU += 1;
    ZU = 1.6;
    if (!v) {
        PX(y, x, "+" + t, 45);
        IU = F(IU, .05);
    }
    if (!(v > 3)) {
        for (let w of RT) {
            for (let u of w.o) {
                if (u == z || u.k || u.N || !(u.m & 4)) {
                    continue;
                }
                let w = IV(u.x - y, u.y - x);
                if (w < 150 + u.r) {
                    u.N = .04 + w / 150 * .1;
                    u.O = v + 1;
                }
            }
        }
        _.F *= .9;
        _.G *= .9;
    }
}

function DZ(z) {
    for (let y of _T) {
        for (let x of y.o) {
            if (x.N && !x.k && !((x.N -= z) > 0)) {
                HZ(x);
                CZ(x, PS, QS, 0, x.O);
            }
        }
    }
}

function EZ(z, y, x, w) {
    for (let v = 0; v < 7; v += 1) {
        let v = C(0, CV);
let u = C(90, 420);
        LX(z, y, GV(v) * u, FV(v) * u, C(.5, 1.1), w, 4, C(3, 8) * (x.t ? 1 : 1.4));
    }
}

function FZ(z) {
    let y = IV(_.F, _.G);
    RT = EY(_.y - 420 - y * z, _.y + 420 + y * z);
    let x = JU;
let w = KU;
    for (let z of RT) {
        if (z.z && _.y >= z.y && _.y < z.y + z.h) {
            DY(z, _.x, _.y);
            x += ER;
            w += FR;
            break;
        }
    }
    if (_.D) {
        _.D.t -= z;
        _.F += x * z;
        _.G += w * z;
        if (_.D.t <= 0) {
W();
}
    } else if (_.C) {} else {
        _.F += x * z;
        _.G += w * z;
    }
    T();
    let v = IV(_.F, _.G);
    if (v > 1400) {
        let y = F(0, 1 - (v / 1400 - 1) * 1.5 * z);
        _.F *= y;
        _.G *= y;
        v *= y;
    }
    let u = J(DV.ceil(v * z / (LV * .55)), 1, 8);
    let t = z / u;
    for (let z = 0; z < u; z += 1) {
        if (_.C) {
            if (!V(t)) {
                _.x += _.F * t;
                _.y += _.G * t;
            }
        } else {
            _.x += _.F * t;
            _.y += _.G * t;
        }
        if (_.D) {
X();
}
        Z(t);
        Y(t);
    }
    CY(_.y);
    if (_.x < CR + LV) {
        _.x = CR + LV;
        if (_.F < 0) {
_.F = .55 * -_.F + 60;
}
    }
    if (_.x > DR - LV) {
        _.x = DR - LV;
        if (_.F > 0) {
_.F = .55 * -_.F - 60;
}
    }
    if (_.E > 0) {
_.E -= z;
}
    if (_.M > 0) {
_.M -= z;
}
    if (_.K > 0) {
        _.K -= z;
        if (_.K <= 0) {
            JU = 0;
            KU = MV;
        }
    }
    _.P = IV(_.F, _.G);
    if (_.P > 30) {
_.a = HV(_.G, _.F);
}
}

function GZ(z, y, x, w, v, u, t) {
    let s = F(3, (u - v) * w / 80 | 0);
    for (let r = 0; r < s; r += 1) {
        let q = _Q(v, u, r / s);
let p = _Q(v, u, (r + 1) / s);
        z.o.push(NS(y + GV(q) * w, x + FV(q) * w, y + GV(p) * w, x + FV(p) * w, t));
    }
}

function HZ(z) {
    PS = z.x;
    QS = z.y;
    RS = 0;
    SS = 0;
    TS = z.g;
    if (z.U) {
        let y = z.U * EU + z.V;
let x = FV(y);
let w = GV(y) * z.U;
        PS += z.W * x;
        QS += z.X * x;
        RS = z.W * w;
        SS = z.X * w;
    }
    if (z.w) {
TS += z.w * EU;
}
}

function IZ(z, y, x) {
    if (z.w) {
        RS += -z.w * (x - QS);
        SS += z.w * (y - PS);
    }
}

function JZ(z, y) {
    return EQ(XT(z, 2)) ? 2 : y && EQ(XT(z, 0)) ? 4 : EQ(XT(z, 1)) ? 8 : EQ(XT(z, 3)) ? 1 : 0;
}

function KZ(z, y) {
    if (!EQ(XT(z, 4))) {
        return 0;
    }
    let x = C(0, CV);
    return {
        W: GV(x) * y,
        X: FV(x) * y * .6,
        U: C(.7, 1.9),
        V: C(0, CV)
    };
}

function LZ(z, y, x) {
    if (!z.t) {
        WS = y - PS;
        XS = x - QS;
        YS = IV(WS, XS);
        if (YS < .0001) {
            WS = 0;
            XS = -1;
            YS = .0001;
        } else {
            WS /= YS;
            XS /= YS;
        }
        US = PS + WS * z.r;
        VS = QS + XS * z.r;
        return z.r;
    }
    let w = GV(TS) * z.r;
let v = FV(TS) * z.r;
    let u = PS - w;
let t = QS - v;
let s = PS + w;
let r = QS + v;
    let q = IX(u, t, s, r, y, x);
    US = u + (s - u) * q;
    VS = t + (r - t) * q;
    WS = y - US;
    XS = x - VS;
    YS = IV(WS, XS);
    if (YS < .0001) {
        WS = -FV(TS);
        XS = GV(TS);
        YS = .0001;
    } else {
        WS /= YS;
        XS /= YS;
    }
    return 5;
}

function MZ(z, y, x, w) {
    for (let v of z.o) {
        PS = v.x;
        QS = v.y;
        TS = v.g;
        if (LZ(v, y, x) + w > YS) {
            return 1;
        }
    }
    return 0;
}

function NZ() {
    let z = ZT;
    let y = ZS(z);
let x = BR(z);
let w = VT[y];
    let v = JV((z + 1400) / XV) > JV(z / XV) && z > 200;
    let u = EQ(.25) ? 120 : 0;
    let t = J(-400 + C(-90, 70) - u, -545, -300);
    let s = J(OV + C(-70, 90) + u, 300, PV);
    let r = v ? 1200 : DQ(700, 1200);
    let q = KS(z, r, t, s, _S, BS);
    q.o.push(NS(_S, z, t, z + r, 0), NS(BS, z, s, z + r, 0));
    ES = F(_S, t) + 30;
    FS = I(BS, s) - 30;
    GS = FS - ES;
    HS = (ES + FS) / 2;
    IS = w;
    JS = x;
    if (v) {
        YZ(q);
    } else {
        let z = +UT[y][5][DQ(0, 9)];
        q.k = z;
        if (CS > 1 && DQ(0, 9) < UT[y][8]) {
q.z = UT[y][7] || DQ(1, 5);
}
        if (z == 1 || z == 5) {
RZ(q, z > 1 ? DQ(2, 3) : 1, z > 1 ? C(0, 26) : C(120, 200), q.y + q.h * C(.3, .6), 0);
} else {
[ QZ, 0, SZ, TZ, UZ, 0, VZ, WZ, XZ ][z](q);
}
        OZ(q, UT[y][6] + (x * 1.6 | 0));
        ZZ(q, y);
    }
    q.i = q.i.filter(z => !MZ(q, z.x, z.y, 10.2));
    _S = t;
    BS = s;
    ZT = z + r;
    CS += 1;
    _T.push(q);
    if (_T.length > 26) {
_T.shift();
}
    return q;
}

function OZ(z, y) {
    for (let x = y * 6; y > 0 && x--; ) {
        let x = C(ES + 40, FS - 40);
let w = z.y + C(.07, .93) * z.h;
        let v = EQ(.6);
let u = C(13, 26);
let t = C(30, 70);
let s = C(0, BV);
        if (!MZ(z, x, w, 48 + (v ? u : t))) {
            y -= 1;
            z.o.push(v ? LS(x, w, u, JZ(IS, 0) | (EQ(.5) ? 1 : EQ(.3) ? WV : 0), KZ(IS, 70)) : MS(x, w, t, s, JZ(IS, 1) | (EQ(.22) ? 16 : 0), KZ(IS, 60)));
            if (z.o[z.o.length - 1].m & WV) {
PZ(z, z.o[z.o.length - 1], 3);
}
        }
    }
}

function PZ(z, y, x) {
    let w = z.T[z.T.length - 1];
    if (!w || w.n >= w.Y) {
        w = {
            n: 0,
            l: 0,
            Y: x,
            x: y.x,
            y: y.y,
            m: []
        };
        z.T.push(w);
    }
    w.n += 1;
    w.m.push(y);
    y.T = w;
    y.m |= WV;
}

function QZ(z) {
    let y = DQ(4, 6);
let x = z.h / (y + 1);
    for (let w = 0; w < y; w += 1) {
        let y = z.y + x * (w + 1);
let v = DQ(4, 7);
        for (let x = 0; x < v; x += 1) {
            z.o.push(LS(ES + 40 + (x + (w & 1 ? .5 : 0)) / (v - .34) * (GS - 90), y, C(14, 30), JZ(IS, 0) | (EQ(.45) ? 1 : EQ(.25) ? WV : 0), KZ(IS, 60)));
            if (z.o[z.o.length - 1].m & WV) {
PZ(z, z.o[z.o.length - 1], 3);
}
        }
    }
}

function RZ(z, y, x, w, v, u) {
    let t = u || JZ(IS, 1);
let s = [];
    for (let z = 0; z < y; z += 1) {
        s.push(ES + (z + C(.22, .8)) * GS / y);
    }
    let r = ES - 20;
let q = w - x;
    for (let v = 0; v <= y; v += 1) {
        let u = v == y;
        let p = F(100, C(120, 200) - JS * 16);
        let o = u ? FS + 20 : s[v] - p / 2;
let n = u ? w - x : w + x;
        if (o - r > 26) {
z.o.push(NS(r, q, o, n, t));
}
        if (!u && x > 40 && EQ(.6)) {
z.o.push(LS(o, n, 15, 1));
}
        r = u ? o : s[v] + p / 2;
        q = w + x;
    }
    if (v) {
        return s;
    }
    for (let y = 0; y < 4; y += 1) {
        z.i.push(OS(0, s[0] + C(-26, 26), w + x + 70 + y * 48));
    }
    z.i.push(OS(EQ(.34) ? 2 : 1, s[y - 1], w - x - 70, FQ(YT(z.Z))));
    return s;
}

function SZ(z) {
    let y = I(GS * .4, 300);
let x = HS + C(-.2, .2) * GS;
let w = z.y + z.h * .6;
    let v = y * GV(BV * .12);
    GZ(z, x, w, y, BV * .12, BV * .8, EQ(.34) ? 2 : 0);
    z.o.push(LS(x - v, w, 15, 1), LS(x + v, w, 15, 1));
    z.i.push(OS(EQ(.3) ? 1 : 2, x, w + y * .5, FQ(YT(z.Z))));
    if (EQ(.6)) {
z.o.push(MS(x, z.y + z.h * .18, C(60, 120), C(-.4, .4), 1, KZ(IS, 90)));
}
}

function TZ(z) {
    let y = F(150, 240 - JS * 30);
let x = HS + C(-.2, .2) * GS;
    let w = C(-90, 90);
    z.o.push(NS(x - y / 2, z.y, x - y / 2 + w, z.y + z.h, 32));
    z.o.push(NS(x + y / 2, z.y, x + y / 2 + w, z.y + z.h, 32));
    for (let y = 0; y < 7; y += 1) {
        z.i.push(OS(0, x + w * (y / 7) + C(-30, 30), z.y + 80 + y * (z.h - 150) / 6));
    }
    if (EQ(.6)) {
        let y = x < HS ? FS - 70 : ES + 70;
        let w = EQ(.5);
        z.i.push(OS(w ? 2 : 4, y, z.y + z.h * .5, w ? FQ(YT(z.Z)) : DQ(0, 6)));
    }
}

function UZ(z) {
    let y = DQ(1, 2);
    for (let x = 0; x < y; x += 1) {
        let w = HS + C(-.22, .22) * GS;
let v = z.y + z.h * (y == 1 ? .5 : .3 + x * .4);
        let u = DQ(2, 3);
let t = I(GS * .3, 200);
let s = GQ() * C(.9, 1.9 + JS * .3);
        for (let y = 0; y < u; y += 1) {
            z.o.push(MS(w, v, t, y * BV / u, 1, {
                w: s
            }));
        }
        z.o.push(LS(w, v, 18, 0));
        z.i.push(OS(0, w, v - t - 40), OS(0, w, v + t + 40));
    }
}

function VZ(z) {
    let y = z.y + z.h * .16;
let x = z.y + z.h * .8;
    RZ(z, 1, 0, y, 1);
    RZ(z, 1, 0, x, 1);
    let w = DQ(4, 7);
    for (let v = 0; v < w; v += 1) {
        let w = ES + C(.12, .8) * GS;
let v = _Q(y + 60, x - 60, GX());
        MZ(z, w, v, 60) || (EQ(.5) ? z.o.push(LS(w, v, C(16, 30), EQ(.5) ? 1 : 16, KZ(IS, 80))) : z.o.push(MS(w, v, C(48, 90), C(0, BV), JZ(IS, 1) | (EQ(.3) ? 32 : 0), KZ(IS, 70))));
    }
    let v = HS;
let u = (y + x) / 2;
let t = I(GS * .3, 200);
    for (let y = 0; y < 4; y += 1) {
        let x = y / 4 * CV + C(0, 1);
let w = v + GV(x) * t;
let s = u + FV(x) * t * .7;
        MZ(z, w, s, 60) || z.o.push(LS(w, s, C(13, 20), 1, KZ(IS, 60)));
    }
    z.i.push(OS(EQ(.22) ? 1 : 2, EQ(.5) ? ES + 60 : FS - 60, (y + x) / 2, FQ(YT(z.Z))));
}

function WZ(z) {
    let y = DQ(2, 3);
    for (let x = 0; x < y; x += 1) {
        let w = z.y + z.h * (.2 + x * .6 / y + C(-.04, .04));
        let v = DQ(3, 5);
let u = I(GS * .8, 300);
let t = HS - u / 2 + C(-40, 40);
        let s = {
            n: v,
            l: 0,
            Y: v,
            x: HS,
            y: w,
            m: []
        };
        z.T.push(s);
        for (let y = 0; y < v; y += 1) {
            let x = MS(t + (y + .5) * u / v, w, 30, BV / 2 + C(-.2, .2), WV);
            x.T = s;
            s.m.push(x);
            z.o.push(x);
        }
        z.o.push(LS(t - 40, w, 20, 1), LS(t + u + 40, w, 20, 1));
        for (let y = 0; y < v; y += 1) {
            z.i.push(OS(0, t + (y + .5) * u / v, w - 60));
        }
    }
    z.i.push(OS(EQ(.4) ? 4 : 2, HS + C(-.3, .3) * GS, z.y + z.h * .92, DQ(0, 6)));
}

function XZ(z) {
    let y = DQ(3, 4);
    for (let x = 0; x < y; x += 1) {
        let w = z.y + z.h * (.15 + x * .8 / y);
        let v = RZ(z, 1, 0, w, 1, 4)[0];
        z.o.push(MS(v, w + z.h * .4 / y, I(GS * .3, 150), C(0, BV), 1, {
            w: GQ() * C(.8, 1.6 + JS * .4)
        }));
        z.i.push(OS(0, v, w + 40), OS(0, v + C(-30, 30), w + 90));
    }
    z.i.push(OS(EQ(.5) ? 1 : 4, HS, z.y + z.h * .96, DQ(0, 6)));
}

function YZ(z) {
    let y = HS;
let x = z.y + z.h * .4;
    UZ(z);
    RZ(z, 1, 0, x + 300, 1);
    z.o.push(LS(y, x, 26, 1));
    for (let w = 0; w < 7; w += 1) {
        z.i.push(OS(2, y + (w - 3) * 70, x + 420, w));
    }
    z.i.push(OS(1, y, z.y + 150));
}

function ZZ(z, y) {
    let x = YT(y);
    let w = UT[y][6] / 6;
    let v = (y, x, w, v) => {
        for (let u = 0; u < 10; u += 1) {
            let u = C(ES + 40, FS - 40);
let t = z.y + C(w, v) * z.h;
            if (!MZ(z, u, t, 30)) {
                z.i.push(OS(y, u, t, x));
                return;
            }
        }
    };
    if (EQ(.8)) {
        let y = DQ(5, 9);
let x = ES + C(.1, .5) * GS;
let w = z.y + C(.16, .5) * z.h;
        let v = GQ() * C(150, 300);
let u = C(-200, 240);
        for (let t = 0; t < y; t += 1) {
            let s = t / y;
            let r = x + v * s;
let q = w + u * FV(s * BV) + s * 150;
            if (r > ES + 26 && r < FS - 26 && !MZ(z, r, q, 26)) {
z.i.push(OS(0, r, q));
}
        }
    }
    if (EQ(w * .6)) {
v(2, EQ(.7) ? FQ(x) : DQ(0, 6), .16, .8);
}
    if (EQ(w * .3)) {
v(EQ(.3) ? 4 : 1, DQ(0, 6), .02, .16);
}
    if (EQ(w * .16)) {
v(4, DQ(0, 6), .3, .8);
}
    if (EQ(w * .07)) {
v(3, 0, .2, .8);
}
}

function _Y(z) {
    DS = z || KV() * 1000000000 | 0;
    CQ = DS >>> 0;
    _T = [];
    ZT = -900;
    _S = -400;
    BS = OV;
    CS = 0;
    let y = KS(-900, 1400, -400, OV, -400, OV);
    y.o.push(NS(-400, -900, -400, 500, 0), NS(OV, -900, OV, 500, 0), NS(-400, -900, OV, -900, 1), LS(-150, 60, 26, 1), LS(150, 240, 26, 1), LS(-60, 420, 20, 1));
    for (let z = 0; z < 7; z += 1) {
        y.i.push(OS(0, z * 70 - 200, 150 + FV(z * .9) * 90));
    }
    y.i.push(OS(2, 240, -60, 1), OS(2, -240, 420, 2));
    _T.push(y);
    ZT = 500;
    while (ZT < 3000) {
        NZ();
    }
}

function BY() {
    while (ZT < _.y + 3200) {
        NZ();
    }
    let z = _T[0];
    if (_.y < z.y + 40) {
        _.y = z.y + 40;
        if (_.G < 0) {
_.G = EV(_.G) * .6;
}
    }
}

function CY(z) {
    CR = -545;
    DR = PV;
    for (let y of _T) {
        if (z >= y.y && z < y.y + y.h) {
            let x = (z - y.y) / y.h;
            CR = _Q(y.$, y.l, x);
            DR = _Q(y._, y.r, x);
            return;
        }
    }
}

function DY(z, y, x) {
    ER = FR = 0;
    let w = z.z;
    if (!w) {
        return;
    }
    let v = (x - z.y) / z.h;
let u = FV(v * 7 + EU * 1.4);
    if (w == 1) {
        let x = GV((y - z.l) / (z.r - z.l) * CV * 1.6);
        if (x > .1) {
FR = -1500 * x;
}
    } else if (w == 2) {
        let w = (z.l + z.r) / 2 - y;
let v = z.y + z.h * .5 - x;
let u = IV(w, v) + 60;
        let t = I(1, 300 / u) * MV * .8;
        ER = w / u * t;
        FR = v / u * t;
    } else if (w == 3) {
        ER = u * MV * .6;
    } else if (w == 4) {
        FR = -2500 * J(I(v, 1 - v) * 6, 0, 1);
    } else if (w == 5) {
        let w = y - (z.l + z.r) / 2;
let v = x - (z.y + z.h * .5);
let u = IV(w, v) + 80;
        let t = 875 * I(1, 300 / u);
        ER = -v / u * t;
        FR = w / u * t;
    } else {
        let w = (z.l + z.r) / 2 + FV(x * .0016 + z.y * .01) * (z.r - z.l) * .3;
        ER = J((w - y) * 5, -1250, MV);
        FR = 625;
    }
}

function EY(z, y) {
    let x = [];
    for (let w of _T) {
        if (w.y + w.h > z && w.y < y) {
x.push(w);
}
    }
    return x;
}

function FY() {
    if (GR) {
        GR.resume();
        return;
    }
    if (!window.AudioContext) {
        return;
    }
    GR = new AudioContext;
    KR = GR.createBiquadFilter();
    KR.type = "lowpass";
    KR.frequency.value = 20000;
    HR = GR.createGain();
    HR.gain.value = HT.m ? 0 : .8;
    KR.connect(HR).connect(GR.destination);
    IR = GR.createGain();
    IR.gain.value = .8;
    IR.connect(KR);
    JR = GR.createGain();
    JR.gain.value = .5;
    JR.connect(KR);
    LR = GR.createBuffer(1, 96000, 48000);
    let z = LR.getChannelData(0);
    for (let y = 0; y < z.length; y += 1) {
        z[y] = KV() * 2 - 1;
    }
    NR = GR.createOscillator();
    NR.type = "sawtooth";
    NR.frequency.value = 200;
    OR = GR.createBiquadFilter();
    OR.type = "bandpass";
    OR.frequency.value = 1400;
    OR.Q.value = 6;
    MR = GR.createGain();
    MR.gain.value = 0;
    NR.connect(OR).connect(MR).connect(IR);
    NR.start();
    QR = GR.currentTime + .1;
}

function GY(z, y, x, w, v, u) {
    z.setValueAtTime(F(u, y), w);
    if (x && x != y) {
z.exponentialRampToValueAtTime(F(u, x), w + v);
}
}

function HY(z, y, x, w, v, u, t) {
    let s = GR.createGain();
    s.gain.setValueAtTime(.0001, x);
    s.gain.exponentialRampToValueAtTime(F(.0001, v), x + t);
    s.gain.exponentialRampToValueAtTime(.0001, x + w);
    (y ? z.connect(y) : z).connect(s).connect(u || IR);
    z.start(x);
    z.stop(x + w + .02);
    PR += 1;
}

function IY(z, y, x, w, v, u, t) {
    if (!VR()) {
        return;
    }
    let s = t || WR();
    let r = GR.createOscillator();
    r.type = z;
    GY(r.frequency, y, x, s, w, 8);
    HY(r, 0, s, w, v, u, .008);
}

function JY(z, y, x, w, v, u, t, s) {
    if (!VR()) {
        return;
    }
    let r = s || WR();
    let q = GR.createBufferSource();
    q.buffer = LR;
    q.loop = 1;
    q.playbackRate.value = .7 + KV() * .6;
    let p = GR.createBiquadFilter();
    p.type = x;
    GY(p.frequency, w, v, r, z, 20);
    p.Q.value = u || 1;
    HY(q, p, r, z, y, t, .005);
}

function KY(z, y, x) {
    if (z < 60) {
        return;
    }
    let w = J(z / 2400, .04, .5);
    JY(.03 + w * .12, w * .7, "lowpass", 300 + z * 1.6, 150, 1);
    if (z > 420) {
IY("triangle", 120 + z * .05, 40, .1 + w * .12, w * .8);
}
    if (y > 1) {
        let z = TR[VU % 4];
let y = EV(x | 0);
        let v = 64 + z[(y >> 5) % z.length] + (y >> 9 & 1) * 12;
        IY("sine", SR(v), SR(v) * 1.6, .16, w * .5);
        IY("triangle", SR(v + 12), 0, .1, w * .3);
    } else {
        if (y) {
IY("sine", 90, 40, .22, w * .5);
}
    }
}

function LY(z) {
    let y = 1 / (1 + z * .5);
    JY(y * .3, y * .5, "bandpass", (1 + z * .3) * 2600, 300, 1.4);
    IY("triangle", (1 + z * .2) * 150, 40, y * .3, y * .5);
    JY(.1, y * .3, "highpass", 4369, 9000, 1);
}

function MY() {
    VY(70, [ 0, 4, 7, 12, 16, 19 ], .5, .12, "square", .04, 12);
    JY(.7, .22, "bandpass", 700, 6000, .8);
    IY("sine", 90, 300, .4, .3);
}

function NY(z) {
    let y = J(z / 2600, .16, .5);
    IY("sawtooth", 150, 700 + z * .2, .2, y * .5);
    JY(.3, y * .7, "bandpass", 700, 4369, 1.2);
    IY("sine", 70, 30, .3, y * .8);
}

function OY(z) {
    IY("square", z ? 700 : 900, z ? 1800 : 1500, .07, .16);
    JY(.05, .12, "highpass", 2600, 5200, 1);
    if (z) {
IY("sine", 300, 900, .16, .2);
}
}

function PY(z) {
    let y = J(z / 1400, .12, .4);
    IY("sine", 180 + z * .1, 700 + z * .3, .1, y);
    IY("sine", 700 + z * .3, 200, .2, y * .8);
}

function QY(z) {
    if (z) {
        IY("triangle", 240, 700, 1.2, .12);
        JY(.06, .1, "bandpass", 900, 2600, 3);
    } else {
        IY("sawtooth", 900, 200, .12, .25);
        JY(.1, .2, "bandpass", 1800, 500, 2);
    }
}

function RY(z) {
    if (GR) {
        if (MR) {
MR.gain.setTargetAtTime(z && !HT.m ? .12 : 0, WR(), .03);
}
        if (z) {
IY("square", 1200, 2600, .05, .1);
}
    }
}

function SY(z) {
    let y = z < 0;
    IY("sine", y ? 120 : 420, y ? 420 : 100, .5, .3);
    IY("triangle", y ? 240 : 700, y ? 700 : 200, .4, .12);
    JY(.4, .1, "lowpass", 900, 200, 1);
}

function TY() {
    IY("sawtooth", 2600, 240, .1, .2);
    JY(.085, .16, "highpass", 7000, 1400, 2);
    IY("sine", 90, 240, .2, .25);
}

function UY(z) {
    let y = 70 + I(z, 14) * 2;
    IY("square", SR(y), 0, .06, .1);
    IY("sine", SR(y + 12), 0, .1, .12);
}

function VY(z, y, x, w, v, u, t) {
    if (!GR) {
        return;
    }
    let s = WR();
    y.forEach((y, r) => IY(v, SR(z + y), t ? SR(z + y + t) : 0, x, w, 0, s + r * u));
}

function WY() {
    VY(70, XR, .5, .12, "triangle", .05);
}

function XY() {
    VY(60, TR[1], .7, .12, "sawtooth", .05, 12);
    VY(90, XR, 1.6, .1, "sine", 0);
    if (GR) {
JY(1.2, .2, "bandpass", 420, 8000, .7, 0, WR() + .3);
}
}

function YY() {
    CX(1);
}

function ZY() {
    CX(0);
}

function _X(z) {
    IY("sine", 70 + z * 40, 48, .16, .12 + z * .16);
}

function BX() {
    VY(70, [ 0, -3, -6, -9, -12, -15, -18 ], .8, .1, "sawtooth", .04, -30);
    JY(1, .3, "lowpass", 3000, 90, 1);
    IY("sine", 120, 30, 1.2, .3);
}

function CX(z) {
    IY("square", z ? 900 : 620, z ? 1200 : 500, .04, .07);
}

function DX() {
    PR = 0;
    if (!GR) {
        return;
    }
    let z = WR();
let y = J(_.P / NV, 0, 1);
let x = H == 1 && _.j;
    let w = (y, x, w) => y.setTargetAtTime(x, z, w);
    w(MR.gain, _.C && !HT.m && x ? .1 + y * .12 : 0, .04);
    w(OR.frequency, 700 + _.P * 1.4, .04);
    w(NR.frequency, 90 + _.P * .2, .04);
    w(KR.frequency, H == 2 ? 700 : 20000, .1);
    w(JR.gain, HT.m ? 0 : H == 1 ? .5 : .3, .2);
    if (x) {
EX();
} else {
QR = z + .1;
}
}

function EX() {
    let z = 40 + VU * 5 % 11;
let y = TR[VU % 4];
let x = UR[VU % 3];
    let w = J(_.P / 1400, 0, 1.6) + (QU > 0 ? .6 : 0);
    let v = 15 / (90 + VU * 7);
    let u = YR[VU];
let t = ZR[VU];
    let s = WR();
    for (let r = 8; QR < s + .16 && r--; ) {
        let r = F(QR, s);
let q = RR;
let p = q & 15;
let o = q >> 4;
        let n = (o * 2 + (p > 7 ? 1 : 0)) % y.length;
        let m = z - 12 + y[n];
        if (u >> p & 1) {
            IY("sine", 120, 40, .16, .5, JR, r);
            JY(.03, .12, "lowpass", 900, 200, 1, JR, r);
        }
        if (t >> p & 1) {
IY(x, SR(m), 0, .2, .3, JR, r);
}
        if (!p) {
            for (let x of [ 0, 3 + (y[2] > 3 ? 1 : 0), 7 ]) {
                IY("triangle", SR(z + y[n] + x), 0, v * 15, .1, JR, r);
            }
        }
        if (w > .22 && p & 1) {
JY(.03, .04 + w * .03, "highpass", 7000, 6000, 1, JR, r);
}
        if (w > .5 && p % 4 == 2) {
JY(.1, .12, "bandpass", 1800, 900, 1.2, JR, r);
}
        if (w > .8 && !(p & 1)) {
            let w = SR(z + 12 + y[(q * 3 + o) % y.length]);
            IY(x, w, w, .1, .1, JR, r);
        }
        RR += 1;
        QR = r + v;
    }
}

function FX(z) {
    let y = DV.imul(z ^ z >>> 15, z | 1);
    y = y + DV.imul(y ^ y >>> 7, y | 61) ^ y;
    return ((y ^ y >>> 14) >>> 0) / 4294967296;
}

function GX() {
    return FX(CQ = CQ + 1831565813 | 0);
}

function HX(z, y) {
    return FX(z * 1000003 + y | 0);
}

function IX(z, y, x, w, v, u) {
    let t = x - z;
let s = w - y;
let r = t * t + s * s;
    return r ? J(((v - z) * t + (u - y) * s) / r, 0, 1) : 0;
}

function JX(z, y, x, w, v, u, t, s) {
    let r = x - z;
let q = w - y;
let p = t - v;
let o = s - u;
    let n = r * o - q * p;
    if (!n) {
        return 0;
    }
    let m = ((v - z) * o - (u - y) * p) / n;
    let l = ((v - z) * q - (u - y) * r) / n;
    return m >= 0 && m <= 1 && l >= 0 && l <= 1 ? [ z + r * m, y + q * m ] : 0;
}

function KX(z, y, x, w) {
    if (GT.length > 26) {
GT.shift();
}
    GT.push({
        x: z,
        y: y,
        r: 0,
        R: x,
        h: w,
        t: 1
    });
}

function LX(z, y, x, w, v, u, t, s) {
    if (CT.length > 420) {
CT.shift();
}
    CT.push({
        x: z,
        y: y,
        F: x,
        G: w,
        l: v,
        L: v,
        h: u,
        k: t,
        s: s,
        a: C(0, CV),
        w: C(-9, 9)
    });
}

function MX(z, y, x, w, v, u, t, s) {
    let r = u ?? UV[NU];
    let q = w;
    for (let w = 0; w < x; w += 1) {
        let x = C(0, CV);
let w = C(.3, 1) * v;
        LX(z, y, (t || 0) + GV(x) * w, (s || 0) + FV(x) * w, C(.16, .5), r + C(-18, 18), q, C(1.4, 3.6));
    }
}

function NX(z, y) {
    LX(z, y, 0, 0, .4, UV[6], 2, 8);
    MX(z, y, 12, 0, 300, UV[6]);
}

function OX(z, y, x) {
    for (let w = 0; w < 7; w += 1) {
        if (z.e & VV[w]) {
MX(y, x, 5, 0, 240, UV[w]);
}
    }
    LX(y, x, 0, 0, .3, UV[z.c], 2, 6);
}

function PX(z, y, x, w) {
    ET.push({
        x: z,
        y: y,
        t: x,
        h: w,
        l: 1.15
    });
}

function QX(z) {
    for (let y = CT.length; y--; ) {
        let x = CT[y];
        if ((x.l -= z) <= 0) {
            CT.splice(y, 1);
        } else {
            x.x += x.F * z;
            x.y += x.G * z;
            if (x.k != 2) {
                x.G += z * 420;
                x.F *= .985;
                x.G *= .985;
            }
            if (x.k == 4) {
x.a += x.w * z;
}
        }
    }
    for (let y = GT.length; y--; ) {
        let x = GT[y];
        x.r += (x.R - x.r) * I(1, z * 9);
        if ((x.t -= z * 2.4) <= 0) {
GT.splice(y, 1);
}
    }
    for (let y = ET.length; y--; ) {
        let x = ET[y];
        x.y -= z * 48;
        if ((x.l -= z) <= 0) {
ET.splice(y, 1);
}
    }
    for (let y = FT.length; y--; ) {
        if ((FT[y].t -= z) <= 0) {
FT.splice(y, 1);
}
    }
}

function RX(z) {
    LU.x = BQ(LU.x, _.x + J(_.F * .12, -180, 180), 7.5, z);
    LU.y = BQ(LU.y, _.y + J(_.G * .22, -124, 136.4), 7.5, z);
    LU.z = BQ(LU.z, 1 / (1 + _.P / 5200), 4, z);
    G = I(DU / 620, CU / 848) * LU.z;
    let y = F(0, 605 - CU / 2 / G);
    LU.x = J(LU.x, -y, y);
    FU = F(0, FU - FU * 7 * z - z * 4);
    let x = I(FU, 26);
    UQ = C(-x, x);
    VQ = C(-x, x);
}

function SX(z) {
    let y = _P(VU);
    for (let x = 0; x < 8; x += 1) {
        TQ[x] = x % 3 || x > 6 ? BQ(TQ[x], y[x], 1.6, z) : ZQ(TQ[x], y[x], 1.6, z);
    }
}

function TX() {
    let z = B.createLinearGradient(0, 0, 0, DU);
    z.addColorStop(0, D(TQ[0] | 0, TQ[1] | 0, TQ[2] | 0));
    z.addColorStop(1, D(TQ[3] | 0, TQ[4] | 0, TQ[5] | 0));
    B.fillStyle = z;
    B.fillRect(0, 0, CU, DU);
    let y = BP[VU];
    B.lineWidth = SQ(2.4);
    for (let z = 0; z < 2; z += 1) {
        let x = z ? .4 : .16;
let w = z ? 300 : 500;
let v = z ? .09 : .05;
        let u = LU.x * x;
let t = LU.y * x;
let s = CU / 2 / G;
let r = DU / 2 / G;
        let q = JV((u + s) / w);
let p = JV((t + r) / w);
        for (let z = JV((t - r) / w); z <= p; z += 1) {
            for (let x = JV((u - s) / w); x <= q; x += 1) {
                let s = HX(x * 30 + y, z * 17);
                if (s < .5) {
                    continue;
                }
                let r = (x + .16 + HX(x, z) * .7) * w;
let q = (z + .16 + HX(z, x + 9) * .7) * w;
                let p = TQ[6] + (s - .5) * 60 | 0;
                B.fillStyle = D(p, 60, TQ[7] | 0, v);
                B.strokeStyle = D(p, 70, TQ[7] | 0, v * 2);
                UX(y, CU / 2 + (r - u) * G, DU / 2 + (q - t) * G, w * (.16 + s * .3) * G, s);
            }
        }
    }
}

function UX(z, y, x, w, v) {
    IQ();
    if (!z) {
        let z = 4 + (v * 4 | 0);
let u = x + .3 * (w *= .6);
        for (let x = 0; x < z; x += 1) {
            let t = x / (z - 1) - .5;
            let s = w * (.3 + .3 * GV(t * 2.4) + HX(x * 7, v * 420) * .16);
            let r = y + t * w * 1.6;
let q = u - s * (.8 + HX(x, v * 90) * .5);
            MQ(r + s, q);
            LQ(r, q, s);
        }
        B.roundRect(y - w * .9, u - w * .3, w * 1.9, w * .3, w * .16);
        B.fill();
        return;
    }
    let u = EV(z);
    for (let z = 0; z < u; z += 1) {
        let t = z / u * CV + v * 4;
        RQ(z, y + GV(t) * w, x + FV(t) * w);
    }
    B.closePath();
    if (z > 0) {
B.fill();
} else {
B.stroke();
}
}

function VX(z) {
    let y = z.m;
    let x = y & WV ? z.S ? 6 : 5 : y & 2 ? 0 : y & 4 ? 1 : y & 8 ? 2 : y & 32 ? 3 : y & 16 ? 4 : -1;
    if (x < 0) {
        return y & 1 ? [ D(TQ[6] + 30 | 0, 90, 48), D(TQ[6] + 40 | 0, 100, 80) ] : [ D(TQ[6] | 0, 48, 19), D(TQ[6] | 0, 90, F(58, TQ[7]) | 0) ];
    }
    let w = CP[x];
    return [ D(w[0], w[1], w[2]), D(w[0], w[3], w[4]) ];
}

function WX() {
    let z = YQ(0) - 200;
let y = YQ(DU) + 200;
    let x = F(2, G * 13);
let w = SQ(5);
    let v = D(TQ[6] | 0, 30, F(4, TQ[2] * .42) | 0);
    for (let u of _T) {
        if (u.y + u.h < z || u.y > y) {
            continue;
        }
        let t = XQ(u.y);
let s = XQ(u.y + u.h);
        B.fillStyle = v;
        for (let z of [ -9000, 9000 ]) {
            IQ();
            MQ(z, t);
            NQ(WQ(z < 0 ? u.$ : u._), t);
            NQ(WQ(z < 0 ? u.l : u.r), s);
            NQ(z, s);
            B.fill();
        }
        if (u.z) {
XX(u);
}
        for (let z of u.o) {
            if (z.k) {
                continue;
            }
            HZ(z);
            let y = VX(z);
            let v = WQ(PS);
let u = XQ(QS);
            let t = z.f > 0 ? z.f = F(0, z.f - .05) : 0;
            let s = z.N > 0 ? 1 : 0;
            if (t || s) {
                B.save();
                B.translate(v, u);
                B.scale(1 + t * .3, 1 + t * .3);
                B.translate(-v, -u);
                if (s) {
B.globalAlpha = .5 + KV() * .5;
}
            }
            if (z.t) {
                let v = GV(TS) * z.r;
let u = FV(TS) * z.r;
                OQ(WQ(PS - v), XQ(QS - u), WQ(PS + v), XQ(QS + u), x, y[0]);
                JQ(w, y[1]);
                if (z.m & 4) {
                    B.setLineDash([ G * 6, G * 7 ]);
                    JQ(SQ(2), D(48, 100, 80, .85));
                    B.setLineDash([]);
                }
                if (t) {
JQ(SQ(2 + t * 5), D(TQ[6] + 30 | 0, 100, 90, t));
}
            } else {
                PQ(v, u, z.r * G, y[0], y[1], SQ(2.4 + t * 4));
                if (z.m & 1) {
PQ(v, u, z.r * G * .5, 0, y[1], SQ(1.6));
}
                if (t) {
PQ(v, u, (z.r + 6 + t * 20) * G, 0, D(TQ[6] + 30 | 0, 100, 80, t), SQ(2));
}
            }
            if (t || s) {
                B.globalAlpha = 1;
                B.restore();
            }
        }
        for (let z of u.T) {
            if (z.l) {
                let y = WQ(z.x);
let x = XQ(z.y) - G * 48;
                for (let w = 0; w < z.n; w += 1) {
                    PQ(y + (w - (z.n - 1) / 2) * 13 * G, x, G * 4, w < z.l ? D(48, 100, 68) : D(48, 30, 32));
                }
            }
        }
        for (let z of u.i) {
            z.g || YX(z);
        }
    }
}

function XX(z) {
    let y = F(z.y, YQ(0) - 100);
let x = I(z.y + z.h, YQ(DU) + 100) - y;
    if (x <= 0) {
        return;
    }
    let w = z.z;
    let v = [ 0, 190, 288, 24, 262, 150, 186 ][w];
    let u = EU * [ 0, -200, 200, 60, -200, 200, 300 ][w];
    for (let w = 0; w < 26; w += 1) {
        let t = HX(w * 13, z.y | 0);
        let s = _Q(z.l + 30, z.r - 30, HX(w, 7));
        let r = y + ((t * x + u) % x + x) % x;
        DY(z, s, r);
        let q = IV(ER, FR);
        if (q < 1) {
            continue;
        }
        let p = I(64, q * .05);
        OQ(WQ(s), XQ(r), WQ(s + ER / q * p), XQ(r + FR / q * p), SQ(2.4), D(v, 90, 70, .16 + t * .16));
    }
}

function YX(z) {
    let y = WQ(z.x);
let x = XQ(z.y);
let w = G;
let v = 1 + FV(EU * 3 + z.x * .02) * .12;
let u = z.t;
    if (!u) {
        PQ(y, x, w * 9 * v, IP, D(38, 100, 84), SQ(2.4));
        return;
    }
    if (u == 3) {
        for (let z = 0; z < 7; z += 1) {
            let u = z / 7 * CV + EU * .6;
            IQ();
            LQ(y, x, w * 26 * v, u, u + .8);
            JQ(w * 7, HQ(z, 60));
        }
        PQ(y, x, w * 13 * v, DP);
        return;
    }
    let t = u == 1;
let s = u == 2;
    let r = s ? z.c : YV[z.c][0];
    let q = t ? 10 : s ? 6 : 3;
let p = (t ? 16 : s ? 12 : 15) * w * v;
    B.save();
    B.translate(y, x);
    B.rotate(EU * (t ? .8 : s ? 1.4 : -1) + z.c);
    QQ(q, t ? IP : s ? HQ(r, 60) : r > 6 ? DP : HQ(r, 60), s ? HQ(r, 90) : DP, SQ(2), z => {
        let y = z / q * CV;
let x = p * (t && z & 1 ? .4 : 1);
        RQ(z, GV(y) * x, FV(y) * x);
    });
    B.restore();
}

function ZX(z, y) {
    let x = [];
    for (let y = 0; y < 7; y += 1) {
        if (z.e & VV[y]) {
x.push(y);
}
    }
    if (x.length < 2) {
        return HQ(x[0] | 0, 60, y);
    }
    let w = B.createLinearGradient(WQ(z.p), XQ(z.q), WQ(z.v), XQ(z.A));
    x.forEach((z, v) => w.addColorStop(v / (x.length - 1), HQ(z, 64, y)));
    return w;
}

function _W() {
    for (let z of BT) {
        let y = z.u ? J(z.l / .22, 0, 1) : 1;
        let x = WQ(z.p);
let w = XQ(z.q);
let v = WQ(z.v);
let u = XQ(z.A);
        OQ(x, w, v, u, F(4, G * 20), ZX(z, y * .16));
        JQ(F(3, G * 15), D(266, 55, 5, y * .8));
        JQ(F(2, G * 10), ZX(z, y));
        JQ(SQ(4), D(0, 0, 100, y * .5));
        if (!z.u) {
            PQ(x, w, (4.5 + FV(EU * 4 + z.p) * .8) * G, 0, HQ(z.c, 80, .9), SQ(2));
            let y = v - x;
let t = u - w;
let s = IV(y, t);
            if (s > 16) {
                let x = y / s;
let w = t / s;
let r = G * 9;
                QQ(3, HQ(z.c, 70, .95), 0, 0, z => {
                    let y = [ 0, 2.4, -2.4 ][z];
let t = GV(y) * r;
let s = FV(y) * r;
                    RQ(z, v + t * x - s * w, u + s * x + t * w);
                });
            }
        }
        if (z == _.C) {
            B.setLineDash([ G * 4, G * 9 ]);
            B.lineDashOffset = 90 * -EU * G;
            JQ(SQ(3), D(190, 100, 88, .9));
            B.setLineDash([]);
        }
    }
    for (let z of FT) {
        let y = z.t / .5;
        PQ(WQ(z.x), XQ(z.y), (6 + (1 - y) * 26) * G, 0, D(0, 0, 100, y), SQ(3));
    }
    if (_.D) {
        let z = WQ(_.D.x);
let y = XQ(_.D.y);
        PQ(z, y, _.D.l * G, 0, HQ(3, 60, .16), SQ(1.4));
        OQ(z, y, WQ(_.x), XQ(_.y), SQ(3.2), HQ(3, 70));
        PQ(z, y, G * 8, HQ(3, 60));
        PQ(z, y, (12 + FV(EU * 9) * 3) * G, 0, HQ(3, 80, .8), SQ(2));
    }
}

function BW() {
    for (let z of GT) {
        let y = J(z.t, 0, 1);
        PQ(WQ(z.x), XQ(z.y), z.r * G, 0, D(z.h | 0, 100, 70, y * .7), SQ(y * 5));
    }
    for (let z of CT) {
        let y = J(z.l / z.L, 0, 1);
let x = WQ(z.x);
let w = XQ(z.y);
        if (z.k == 4) {
            B.save();
            B.translate(x, w);
            B.rotate(z.a);
            let v = z.s * G * (.4 + y * .6);
            QQ(4, D(z.h | 0, 70, 40 + y * 26, y), D(z.h | 0, 90, 70, y), SQ(1.2), z => RQ(z, [ -1, .8, 1.2, -.6 ][z] * v, [ -.7, -1.2, .9, 1 ][z] * v));
            B.restore();
            continue;
        }
        if (z.k == 2) {
PQ(x, w, (1 - y) * z.s * 9 * G, 0, D(z.h | 0, 100, 70, y), SQ(y * 3));
} else {
OQ(x, w, x - z.F * (z.k ? .02 : .012) * G, w - z.G * (z.k ? .02 : .012) * G, SQ(z.s * (z.k ? 1 : y)), D(z.h | 0, z.k ? 70 : 100, 60, y));
}
    }
    for (let z of ET) {
        let y = J(z.l, 0, 1);
        GW(z.t, WQ(z.x), XQ(z.y), G * 17 / E, z.h < 0 ? D(0, 0, 100, y) : D(z.h | 0, 100, 70, y), 1);
    }
}

function CW() {
    let z = DT.length ? DT[DT.length - 1] : 0;
    if (!z || IV(_.x - z.x, _.y - z.y) > 7) {
        DT.push({
            x: _.x,
            y: _.y,
            h: QU > 0 ? -1 : UV[NU]
        });
        while (DT.length > 26 + J(_.P / 30, 0, 30)) {
            DT.shift();
        }
    }
}

function DW() {
    let z = DT.length;
    for (let y = 1; y < z; y += 1) {
        let x = y / z;
let w = DT[y - 1];
let v = DT[y];
        OQ(WQ(w.x), XQ(w.y), WQ(v.x), XQ(v.y), F(1, x * 15 * G * 1), D((v.h < 0 ? (EU * 400 + y * 26) % 360 : v.h) | 0, 100, 60 + x * 18, x * .8));
    }
}

function EW(z, y, x, w) {
    let v = x ? DP : D(300, 40, 96);
    let u = x ? HQ(JV(EU * 6) % 7, 70) : D(280, 45, 62);
    let t = FV(EU * 13);
let s = JV(EU * 5);
    IQ();
    MQ(-13, 0);
    for (let z = 1; z < 5; z += 1) {
        NQ(-13 - z * 6, FV(EU * 9 - z * .8) * (2 + z * 1.6));
    }
    JQ(5, HQ((s + 2) % 7, 60));
    JQ(2.4, HQ((s + 5) % 7, 80));
    for (let z = 0; z < 4; z += 1) {
        let y = (z >> 1) * 15 - 8;
let x = t * (z & 1 ? 1 : -1) * (z > 1 ? -1 : 1);
        IQ();
        MQ(y, 6);
        NQ(y + x * 5, 15);
        NQ(y + x * 8 + 2, 20);
        JQ(3, v);
    }
    IQ();
    B.ellipse(0, 0, 17, 11, 0, 0, CV);
    KQ(v);
    JQ(2, u);
    IQ();
    MQ(8, -6);
    B.quadraticCurveTo(20, -14, 26, -12);
    B.quadraticCurveTo(30, -11, 30, -5);
    NQ(20, -1);
    B.closePath();
    KQ(v);
    JQ(2, u);
    for (let z = 0; z < 4; z += 1) {
        IQ();
        MQ(6 + z * 4, -7 - z);
        B.quadraticCurveTo(2 + z * 4, -16 - z * 1.6, 9 + z * 4, -18 - z);
        JQ(3.2, HQ((s + z) % 7, 60));
    }
    B.save();
    B.translate(26, -12);
    B.rotate(-.6);
    let r = B.createLinearGradient(0, 0, 0, -15);
    r.addColorStop(0, HQ(0, 60));
    r.addColorStop(1, HQ(y, 90));
    QQ(3, r, DP, 1.2, z => RQ(z, [ -3, 3, 0 ][z], [ 0, 0, -15 ][z]));
    for (let z = 0; z < 4; z += 1) {
        let y = z / 4;
        OQ(y * 6 * .5 - 3, -15 * y, 3 - y * 6 * .5, -15 * y - 2, 1.2, HQ((s + z) % 7, 90));
    }
    B.restore();
    PQ(26, -8, 1.6, H == 1 && _.aa > TV ? D(0, 100, 63) : D(276, 38, 15));
}

function FW() {
    let z = G;
let y = WQ(_.x);
let x = XQ(_.y);
    B.save();
    B.translate(y, x);
    B.rotate(_.a);
    B.scale(z, z);
    EW(0, NU, QU > 0 || _.E > 0);
    B.restore();
    PQ(y, x, z * 24, 0, _.M > 0 ? D(6, 100, 62) : HQ(NU, 70, .5), SQ(_.M > 0 ? 3 : 2));
    if (_.aa > TV) {
        let w = (_.aa - TV) / 1.5;
        IQ();
        LQ(y, x, (33 + FV(EU * 20) * 4) * z, -BV / 2, -BV / 2 + CV * (1 - w));
        JQ(F(2, z * 4), D(30 - w * 30, 100, 60));
    }
}

function GW(z, y, x, w, v, u, t) {
    B.font = (u ? "bold " : "") + (w * E | 0) + "px monospace";
    B.textAlign = t || "center";
    B.fillStyle = v;
    B.fillText(z, y, x);
}

function HW(z, y, x, w, v, u, t) {
    let s = LP(z, y, x, w);
    JP.push({
        x: z,
        y: y,
        w: x,
        h: w,
        ba: u
    });
    KP(z - x / 2, y - w / 2, x, w, E * 8);
    KQ(s ? t || HP : GP);
    JQ(E * 2, s ? DP : HP);
    GW(v, z, y + E, 15, s ? DP : EP, 1);
}

function IW() {
    for (let z of JP) {
        if (LP(z.x, z.y, z.w, z.h)) {
            CX(1);
            z.ba();
            return 1;
        }
    }
    return 0;
}

function JW() {
    let z = E * 30;
let y = E * 240;
let x = F(_.y, 0);
    PQ(z, z, E * 9, IP);
    GW(RU, z + E * 16, z, 16, IP, 1, "left");
    GW(SU | 0, CU - z, z, 19, DP, 1, "right");
    if (TU > 1.05) {
GW("x" + TU.toFixed(1), CU - z, z + E * 22, 15, HQ(2, 70), 1, "right");
}
    if (YU > 3) {
        let y = 1 + I(YU, 30) * .02;
let x = J(ZU / 1.6, 0, 1);
        GW(YU + " CHAIN", CU - z, z + E * 46, y * 15, D(48, 100, 60 + YU, x), 1, "right");
    }
    B.fillStyle = FP;
    B.fillRect(CU / 2 - y / 2, z + E * 12, y, E * 4);
    B.fillStyle = D(TQ[6] | 0, 90, 70);
    B.fillRect(CU / 2 - y / 2, z + E * 12, y * J(x % XV / XV, 0, 1), E * 4);
    GW(UT[VU][0] + (_R(_.y) ? " +" + _R(_.y) : ""), CU / 2, E * 18, 14, EP, 1);
    GW((x / 10 | 0) + "m", CU / 2 + y / 2 + E * 24, z + E * 14, 13, FP, 0, "left");
    KW();
    let w = 0;
    for (let y = 0; y < 7; y += 1) {
        if (XU[y] > 0) {
GW(ZV[y] + " " + XU[y].toFixed(1), CU - z, z + (30 + 16 * w++) * E, 12, YV[y][0] > 6 ? DP : HQ(YV[y][0], 70), 1, "right");
}
    }
    if (WU > 0) {
GW(UT[VU][0], CU / 2, DU * .3, 42, D(0, 0, 100, J(WU, 0, 1) * J(3.2 - WU, 0, 1)), 1);
}
}

function KW() {
    let z = E * 44;
let y = CU / 2 - z * 25 / 7;
let x = DU - E * 40;
let w = E * 26;
    for (let v = 0; v < 7; v += 1) {
        let u = y + v * z * 50 / 44;
let t = v == NU;
let s = MU[v] / RV;
let r = u + z / 2;
        KP(u, x - w / 2, z, w, E * 5);
        KQ(D(270, 30, 8, .8));
        B.save();
        B.clip();
        B.fillStyle = HQ(v, t ? 60 : 42, OT == v && PT > 0 ? .4 + FV(EU * 40) * .3 : 1);
        B.fillRect(u, x + w / 2 - w * s, z, w * s);
        B.restore();
        KP(u, x - w / 2, z, w, E * 5);
        JQ((t ? 2.6 : 1.2) * E, t ? DP : HQ(v, 55, .8));
        GW("ROYGBIV"[v], r, x, t ? 15 : 12, t ? DP : EP, 1);
        GW(v + 1, r, x - w / 2 - E * 9, 10, t ? DP : FP);
        if (OU & VV[v]) {
PQ(r, x + w / 2 + E * 8, E * 2.6, HQ(v, 75));
}
        JP.push({
            x: r,
            y: x,
            w: z,
            h: w,
            ba: () => YW(v)
        });
    }
    if (PU > 2) {
GW(PU + "/7", y - E * 18, x, 13, DP, 1);
}
}

function LW() {
    if (H != 1 || MT || !_.j) {
        return;
    }
    let z = KT - _.x;
let y = LT - _.y;
let x = IV(z, y);
    if (x > QV) {
PQ(WQ(_.x + z / x * QV), XQ(_.y + y / x * QV), E * 4, HQ(NU, 60, .8));
}
}

function MW(z, y, x, w, v, u) {
    B.fillStyle = D(275, 45, 5, .8);
    B.fillRect(0, 0, CU, DU);
    if (z) {
        KP(CU / 2 - z * E / 2, DU / 2 - y * E / 2, z * E, y * E, E * 14);
        KQ(D(272, 40, 9, .96));
        JQ(E * 2, HP);
        GW(x, CU / 2, DU / 2 - (y / 2 - 34) * E, 27, DP, 1);
    }
    for (let z = 0; z < w.length; z += 1) {
        GW(w[z], CU / 2, DU / 2 - (y / 2 - 78) * E + z * 22 * E, u || 13.5, EP, u ? 1 : 0);
    }
    for (let z of v) {
        HW(CU / 2 + z[0] * E, DU / 2 + z[1] * E, z[2] * E, E * 40, z[3], z[4], z[5]);
    }
}

function NW() {
    HT.m ^= 1;
    if (HR) {
HR.gain.value = HT.m ? 0 : .8;
}
    RW();
}

function OW() {
    MW(0, 0, 0, [], [ [ 0, -30, 190, "PLAY", SW, D(300, 80, 55) ], [ 0, 26, 130, HT.m ? "UNMUTE" : "MUTE", NW ] ]);
    let z = DU * .3;
    for (let y = 0; y < 7; y += 1) {
        GW("PRISMFALL", CU / 2 + FV(EU * 1.2 + y * .5) * 5 * E, z + (y - 3) * 2.4 * E, 66, HQ(y, 60, .5), 1);
    }
    GW("PRISMFALL", CU / 2, z, 66, DP, 1);
    [ "DRAG near the unicorn - drawings STAY until used - longer = stronger", "R push - O aim - Y spring - G tether - B rail - I gravity - V warp", 0, "X lets go - 1-7 or SCROLL picks a colour" ].forEach((z, y) => z && GW(z, CU / 2, DU - E * 122 + y * 19 * E, 13, y == 1 ? EP : y > 2 ? FP : DP, !y));
    let y = B.createLinearGradient(CU / 2 - E * 300, 0, CU / 2 + E * 300, 0);
    for (let z = 0; z < 7; z += 1) {
        y.addColorStop(z / 6, HQ(z, 68));
    }
    GW("CROSS TWO STROKES TO MIX THEM - O+Y aims AND springs", CU / 2, DU - E * 84, 13, y, 1);
    if (HT.b) {
GW("BEST " + HT.b + "   DEPTH " + (HT.d / 10 | 0) + "m", CU / 2, DU / 2 + E * 82, 14, FP);
}
}

function PW() {
    MW(470, 330, "RUN OVER", [ "SCORE   " + (SU | 0), "DEPTH   " + (UU / 10 | 0) + "m", "REGION  " + UT[VU][0], "COINS   +" + RU, "BEST    " + HT.b, SU >= HT.b && SU ? "NEW BEST!" : "" ], [ [ 0, 62, 210, "RETRY  (R)", SW, D(300, 80, 55) ], [ 0, 114, 130, "MENU", () => {
        H = 0;
    } ] ], 16);
}

function QW() {
    MW(340, 296, "PAUSED", [], [ [ 0, -40, 200, "RESUME", () => {
        H = 1;
    } ], [ 0, 8, 200, "RESTART  (R)", SW ], [ 0, 56, 200, HT.m ? "UNMUTE" : "MUTE", NW ], [ 0, 104, 200, "QUIT RUN", () => {
        TW();
        H = 3;
    } ] ]);
}

function RW() {
    try {
        localStorage[_U] = [ HT.b, HT.d, HT.m ].join();
    } catch (z) {}
}

function SW(z) {
    FY();
    H = 1;
    SU = 0;
    RU = 0;
    TU = 1;
    UU = 0;
    VU = 0;
    WU = 3.2;
    OU = 0;
    PU = 0;
    NT = 0;
    QU = 0;
    YU = 0;
    ZU = 0;
    OP = 0;
    FU = 0;
    GU = 0;
    IU = 0;
    MU = [ RV, RV, RV, RV, RV, RV, RV ];
    XU = [ 0, 0, 0, 0, 0, 0, 0 ];
    BT = [];
    CT = [];
    DT = [];
    ET = [];
    FT = [];
    GT = [];
    MT = 0;
    _.x = 0;
    _.y = -320;
    _.G = 300;
    _.a = BV / 2;
    _.C = 0;
    _.D = 0;
    _.E = 0;
    _.M = 0;
    _.aa = 0;
    _.K = 0;
    _.j = 1;
    _.P = 300;
    _.I = 0;
    JU = 0;
    KU = MV;
    LU.x = _.x;
    LU.y = _.y;
    LU.z = 1;
    TQ = _P(0);
    _Y(z);
    _.F = C(-70, 70);
    RR = 0;
    if (GR) {
QR = GR.currentTime + .05;
}
    RY(0);
}

function TW() {
    HT.b = F(HT.b, SU | 0);
    HT.d = F(HT.d, UU | 0);
    RW();
    RY(0);
}

function UW() {
    _.j = 0;
    OP = 1.3;
    for (let z = 0; z < 7; z += 1) {
        MX(_.x, _.y, 16, 0, 520, UV[z]);
    }
    LX(_.x, _.y, 0, 0, .7, -1, 2, 22);
    FU = 30;
    GU = .7;
    HU = -1;
    BX();
    if (_.C) {
U(0);
}
    if (_.D) {
W();
}
    if (H != 0) {
TW();
}
}

function VW(z) {
    BY();
    if (_.j) {
WW(z);
}
    DZ(z);
    let y = ZS(_.y);
    if (y != VU) {
        VU = y;
        WU = 3.2;
        GU = F(GU, .35);
        HU = UT[VU][1];
        MY();
        SU += TU * 500 | 0;
    }
    if (_.y > UU) {
        SU += (_.y - UU) * .05 * TU;
        UU = _.y;
    }
    TU = J(1 + _.P / 950 + PU * .2 + (QU > 0 ? 2 : 0), 1, 12);
    if (_.j) {
        let y = 2.4;
        if (_.P < 118) {
            _.aa += z;
            let x = J((_.aa - TV) / (y - TV), 0, 1);
            if ((PP -= z) <= 0 && x > 0) {
                PP = F(.16, .55 - x * .4);
                _X(x);
            }
            if (_.aa > y) {
UW();
}
        } else if (_.aa > 0) {
            if (_.aa > TV) {
MX(_.x, _.y, 8, 0, 260, UV[0]);
}
            _.aa = F(0, _.aa - z * 2.6);
        }
    } else {
        OP -= z;
        if (OP <= 0) {
            if (H == 0) {
                SW();
                H = 0;
            } else {
                H = 3;
            }
        }
    }
    WU -= z;
    PT -= z;
    QU -= z;
    for (let y = 0; y < 7; y += 1) {
        XU[y] -= z;
    }
    if (ZU > 0 && (ZU -= z) <= 0) {
YU = 0;
}
    if (NT > 0 && (NT -= z) <= 0) {
        OU = 0;
        PU = 0;
    }
    GU = F(0, GU - z * 2.4);
    for (let y = BT.length; y--; ) {
        let x = BT[y];
        if (x.u) {
x.l -= z;
}
        if (x.u && x.l <= 0 || x.A < _.y - 2600) {
            if (_.C == x) {
U(1);
}
            if (x == MT) {
MT = 0;
}
            BT.splice(y, 1);
        }
    }
    QX(z);
    if (_.j) {
CW();
}
}

function WW(z) {
    MP += z;
    let y = 0;
    while (MP >= QP && y < 8) {
        FZ(QP);
        MP -= QP;
        y += 1;
    }
    if (y >= 8) {
MP = 0;
}
}

function XW() {
    JP = [];
    E = I(CU / 1280, DU / 720);
    TX();
    WX();
    _W();
    DW();
    BW();
    if (_.j) {
FW();
}
    H != 1 && H != 2 || JW();
    if (GU > .01) {
        B.fillStyle = HU < 0 ? D(EU * 500 % 360 | 0, 100, 75, GU * .5) : D(HU | 0, 100, 72, GU * .45);
        B.fillRect(0, 0, CU, DU);
    }
    if (H > 1) {
[ 0, 0, QW, PW, 0 ][H]();
} else {
H || OW();
}
    LW();
}

function YW(z) {
    if ((z = (z + 7) % 7) != NU) {
        NU = z;
        CX(1);
        MX(_.x, _.y, 6, 0, 130, UV[z]);
    }
}

function ZW() {
    let z = I(devicePixelRatio, 2);
    CU = BU.width = innerWidth * z | 0;
    DU = BU.height = innerHeight * z | 0;
    B.lineCap = "round";
    B.textBaseline = "middle";
}

function _V(z) {
    let y = CU / innerWidth;
    IT = z.clientX * y;
    JT = z.clientY * y;
    KT = LU.x + (IT - CU / 2 - UQ) / G;
    LT = YQ(JT);
}

BV = Math.PI;

CV = BV * 2;

DV = Math;

EV = DV.abs;
I = DV.min;
F = DV.max;
FV = DV.sin;
GV = DV.cos;
HV = DV.atan2;
IV = DV.hypot;
JV = DV.floor;
KV = DV.random;

LV = 17;

MV = 1250;

NV = 2900;

OV = 400;

PV = 545;

QV = 305;

RV = 100;

SV = [ .088, .055, .062, .07, .05, .076, .095 ];

TV = .9;

UV = [ 0, 32, 54, 140, 200, 248, 312 ];

VV = [ 1, 2, 4, 8, 16, 32, 64 ];

WV = 64;

XV = 15000;

YV = [ [ 0, 1.5, 10 ], [ 2, 1.5, 11 ], [ 3, 1.7, 12 ], [ 4, 2, 12 ], [ 5, 1.9, 11 ], [ 6, 1.9, 11 ], [ 7, .4, 12 ] ];

ZV = "OVERDRIVE SUPERCOIL REACH SUPERRAIL FLUX ECHO EFFICIENCY".split(" ");

_U = "pf26_save";

BU = document.querySelector("canvas");

B = BU.getContext("2d");

CU = 0;
DU = 0;
G = 1;

H = 0;

EU = 0;

FU = 0;

GU = 0;

HU = 0;

IU = 0;

_ = {
    x: 0,
    y: 0,
    F: 0,
    G: 0,
    a: 0,
    P: 0,
    C: 0,
    H: 0,
    J: 1,
    I: 0,
    D: 0,
    E: 0,
    M: 0,
    aa: 0,
    K: 0,
    j: 1
};

JU = 0;
KU = MV;

LU = {
    x: 0,
    y: 0,
    z: 1,
    ca: 1
};

MU = [];

NU = 1;

OU = 0;

PU = 0;

QU = 0;

RU = 0;

SU = 0;

TU = 1;

UU = 0;

VU = 0;

WU = 0;

XU = [];

YU = 0;
ZU = 0;

_T = [];

BT = [];

CT = [];

DT = [];

ET = [];

FT = [];

GT = [];

HT = {
    b: 0,
    d: 0,
    m: 0
};

IT = 0;
JT = 0;

KT = 0;
LT = 0;

MT = 0;

NT = 0;

OT = -1;
PT = 0;

QT = z => .45 + I(z, 210) / 105 * .55;

RT = [];

ST = 0;

TT = z => z.m & 2 ? 280 : z.m & 4 ? 20 : z.m & 8 ? 285 : TQ[6];

UT = [ [ "CLOUDBREAK", 206, 74, 88, 26, "0002100210", 5, 1, 5, 40 ], [ "SUNFORGE", 12, 34, 72, 19, "8574185741", 7, 3, 4, 196 ], [ "VERDANT COIL", 152, -74, 74, 17, "2622762262", 8, 5, 6, 320 ], [ "CRYSTAL CURRENT", 190, 44, 84, 15, "3303531333", 5, 6, 7, 25 ], [ "PRISM MINE", 276, -28, 62, 10, "6760671666", 9, 2, 6, 120 ], [ "INVERSION TEMPLE", 264, 52, 80, 13, "6465264636", 7, 4, 7, 75 ], [ "RAINBOW ENGINE", 300, -136, 90, 9, "0123456788", 9, 0, 9, -1 ] ];

VT = [ 180, 786794, 131494, 69971, 693042, 303957, 414347 ];

WT = "120132410656";

XT = (z, y) => (z >> 16 - y * 4 & 15) / 16;

YT = z => z > 5 ? [ DQ(0, 6), DQ(0, 6) ] : [ +WT[z * 2], +WT[z * 2 + 1] ];

ZT = 0;
_S = -400;
BS = OV;
CS = 0;
DS = 1;

ES = 0;
FS = 0;
GS = 0;
HS = 0;
IS = 0;
JS = 0;

KS = (z, y, x, w, v, u) => ({
    y: z,
    h: y,
    l: x,
    r: w,
    $: v,
    _: u,
    o: [],
    i: [],
    Z: ZS(z),
    k: 0,
    z: 0,
    T: []
});

LS = (z, y, x, w, v) => ({
    t: 0,
    x: z,
    y: y,
    r: x,
    g: 0,
    m: w | 0,
    ...v
});

MS = (z, y, x, w, v, u) => ({
    t: 1,
    x: z,
    y: y,
    r: x,
    g: w,
    m: v | 0,
    ...u
});

NS = (z, y, x, w, v, u) => MS((z + x) / 2, (y + w) / 2, IV(x - z, w - y) / 2, HV(w - y, x - z), v, u);

OS = (z, y, x, w) => ({
    t: z,
    x: y,
    y: x,
    c: w | 0,
    g: 0
});

PS = 0;
QS = 0;
RS = 0;
SS = 0;
TS = 0;

US = 0;
VS = 0;
WS = 0;
XS = 0;
YS = 0;

ZS = z => {
    let y = JV(F(z, 0) / XV);
    return y < 7 ? y : y % 7;
};

_R = z => JV(JV(F(z, 0) / XV) / 7);

BR = z => J(JV(F(z, 0) / XV) * .085 + _R(z) * .3, 0, 2.4);

CR = -545;
DR = PV;

ER = 0;
FR = 0;

GR = 0;
HR = 0;
IR = 0;
JR = 0;
KR = 0;
LR = 0;
MR = 0;
NR = 0;
OR = 0;

PR = 0;

QR = 0;
RR = 0;

SR = z => 420 * DV.pow(2, (z - 70) / 12);

TR = [ [ 0, 2, 3, 5, 7, 8, 10 ], [ 0, 2, 4, 5, 7, 9, 11 ], [ 0, 3, 5, 7, 10, 12 ], [ 0, 1, 5, 6, 8, 11 ] ];

UR = [ "triangle", "sawtooth", "square" ];

VR = () => GR && !HT.m && PR < 26;

WR = () => GR.currentTime;

XR = [ 0, 4, 7, 12 ];

YR = [ 4369, 4437, 4115, 4369, 4097, 1298, 21845 ];

ZR = [ 34953, 42149, 37266, 52429, 33924, 43177, 43691 ];

J = (z, y, x) => z < y ? y : z > x ? x : z;

_Q = (z, y, x) => z + (y - z) * x;

BQ = (z, y, x, w) => _Q(z, y, 1 - DV.exp(-x * w));

CQ = 1;

C = (z, y) => z + GX() * (y - z);

DQ = (z, y) => JV(z + GX() * (y - z + 1));

EQ = z => GX() < z;

FQ = z => z[JV(GX() * z.length)];

GQ = () => GX() < .5 ? -1 : 1;

D = (z, y, x, w = 1) => "hsl(" + z + " " + y + "% " + x + "% / " + w + ")";

HQ = (z, y, x) => D(UV[z], 100, y, x);

IQ = () => B.beginPath();

JQ = (z, y) => {
    B.lineWidth = z;
    B.strokeStyle = y;
    B.stroke();
};

KQ = z => {
    B.fillStyle = z;
    B.fill();
};

LQ = (z, y, x, w, v) => B.arc(z, y, x, w || 0, v ?? CV);

MQ = (z, y) => B.moveTo(z, y);

NQ = (z, y) => B.lineTo(z, y);

OQ = (z, y, x, w, v, u) => {
    IQ();
    MQ(z, y);
    NQ(x, w);
    JQ(v, u);
};

PQ = (z, y, x, w, v, u) => {
    IQ();
    LQ(z, y, x);
    if (w) {
KQ(w);
}
    if (v) {
JQ(u, v);
}
};

QQ = (z, y, x, w, v) => {
    IQ();
    for (let y = 0; y < z; y += 1) {
        v(y);
    }
    B.closePath();
    if (y) {
KQ(y);
}
    if (x) {
JQ(w, x);
}
};

RQ = (z, y, x) => (z ? NQ : MQ)(y, x);

SQ = z => F(1, z * G);

TQ = [ 232, 62, 12, 322, 52, 27, 288, 80 ];

UQ = 0;
VQ = 0;

WQ = z => CU / 2 + (z - LU.x) * G + UQ;

XQ = z => DU / 2 + (z - LU.y) * G + VQ;

YQ = z => LU.y + (z - DU / 2 - VQ) / G;

ZQ = (z, y, x, w) => (z + ((y - z + 540) % 360 - 180) * (1 - DV.exp(-x * w)) + 360) % 360;

_P = z => {
    let y = UT[z];
let x = y[1];
let w = y[4];
let v = y[9];
    return [ x, 56, w, x + y[2], 64, w + 23, v < 0 ? EU * 16 % 360 : v, y[3] ];
};

BP = [ 0, -6, -3, 5, 7, -8, -4 ];

CP = [ [ 276, 40, 7, 60, 26 ], [ 18, 74, 42, 100, 66 ], [ 292, 60, 30, 100, 74 ], [ 200, 80, 40, 100, 76 ], [ 140, 70, 34, 100, 70 ], [ 46, 38, 22, 90, 60 ], [ 46, 100, 60, 100, 92 ] ];

DP = D(0, 0, 100);

EP = D(0, 0, 100, .62);

FP = D(0, 0, 100, .34);

GP = D(282, 40, 15, .88);

HP = D(290, 55, 50);

IP = D(48, 100, 66);

E = 1;

JP = [];

KP = (z, y, x, w, v) => {
    IQ();
    B.roundRect(z, y, x, w, v);
};

LP = (z, y, x, w) => IT > z - x / 2 && IT < z + x / 2 && JT > y - w / 2 && JT < y + w / 2;

MP = 0;
NP = 0;
OP = 0;
PP = 0;

QP = 1 / 120;

(() => {
    try {
        let z = (localStorage[_U] || "").split(",").map(Number);
        if (z.length < 3 || z.some(isNaN)) {
            return;
        }
        HT.b = J(z[0] | 0, 0, 1000000000000);
        HT.d = J(z[1] | 0, 0, 1000000000000);
        HT.m = z[2] ? 1 : 0;
    } catch (z) {}
})();

ZW();

SW();

H = 0;

_.j = 1;

requestAnimationFrame(function z(y) {
    requestAnimationFrame(z);
    let x = J((y - NP) / 1000, 0, .05);
    NP = y;
    let w = x;
    if (IU > 0) {
        IU -= x;
        w *= .12;
    }
    EU += w;
    if (H == 1 || H == 0) {
VW(w);
} else {
if (H == 3) {
QX(w);
}
}
    RX(x);
    SX(x);
    DX();
    XW();
});

BU.style.cssText = "position:fixed;inset:0;width:100%;height:100%";

addEventListener("resize", ZW);

addEventListener("contextmenu", z => z.preventDefault());

addEventListener("pointermove", z => {
    _V(z);
    if (MT) {
M();
}
});

addEventListener("pointerdown", z => {
    FY();
    _V(z);
    z.button || IW() || H != 1 || L();
});

addEventListener("pointerup", () => {
    MT = 0;
});

addEventListener("blur", () => {
    MT = 0;
    if (H == 1) {
H = 2;
}
});

addEventListener("wheel", z => {
    if (H == 1) {
YW(NU + (z.deltaY > 0 ? 1 : -1));
}
});

addEventListener("keydown", z => {
    FY();
    let y = z.key;
let x = y.toLowerCase();
    if (y > "0" && y < "8") {
        YW(+y - 1);
    } else {
        y != "Escape" && x != "p" || (H = H == 1 ? 2 : H == 2 ? 1 : H);
        if (x == "r" && H > 1) {
SW();
}
        if (x == "m") {
NW();
}
        if (x == "x") {
if (_.C) {
U(1);
} else {
if (_.D) {
W();
}
}
}
        y != " " && y != "Enter" || (H == 2 ? H = 1 : H < 4 && SW());
    }
});

addEventListener("visibilitychange", () => {
    if (H == 1) {
H = 2;
}
});